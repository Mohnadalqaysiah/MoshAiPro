# سجل القرارات — Mosh AI Pro v5 / Qaffel AI

> **ما هذا الملف:** القرارات التي لا تُستنتج من الكود. الكود يقول *ماذا* يفعل
> النظام، وسجل git يقول *متى* تغيّر — وكلاهما لا يقول **لماذا اختير هذا دون
> غيره، وما البديل الذي رُفض ولماذا**. تلك المعرفة تضيع بانتهاء الجلسة، فيُعاد
> فتح نقاش محسوم أو — أسوأ — يُنقض قرار لأسبابه ما زالت قائمة.
>
> **يُقرأ قبل أي تعديل** على المحرك أو حلقة الرصد أو المدفوعات أو التقارير.

---

## قاعدة التحديث

يُضاف قيد **في نفس اللحظة** التي يُتخذ فيها القرار، لا في نهاية العمل، متى
تحقّق أحد هذه:

- تعديل يمسّ **توليد الإشارات** أو **قياس نتائجها**
- قرار يتعلق بـ**المدفوعات** أو الباقات أو الصلاحيات
- **تراجع** عن قرار سابق — يُوسم الأصل `متراجَع عنه` ولا يُحذف
- **استنتاج أُبطل بالقياس** — الاستنتاج الخاطئ يُسجَّل مع سبب بطلانه، وإلا
  أُعيد اكتشافه لاحقاً وبُني عليه من جديد
- **قيد تقني** يحدّ ما يمكن عمله (نافذة بيانات، حدّ API، بنية قديمة)

**لا يُسجَّل هنا:** ما يقوله الكود بوضوح، ولا إصلاحات الأخطاء الروتينية،
ولا تفاصيل تخصّ جلسة واحدة.

**الصيغة:** الأحدث أولاً. كل قيد يحمل حالته: `مطبَّق` / `معلّق` / `متراجَع عنه`.

---

## 2026-09-17

### تأكيد تعديل الأهداف — بمرجع صحيح للمعادن
**الحالة:** `مطبَّق ومؤكَّد`

أُعيدت المحاكاة بعد تصحيح مرجع المعادن (228 قراراً، شموع 5m، الفضة
والذهب من `OANDA` الفوري بتغطية 26 يوماً):

```
                          مُتداوَلة  نسبة الربح   التوقّع   الثبات
هدف أول عند 1.0R              209       43.5%     +0.15R   ✓ 49% ← 41%
وقف ≥0.50% + هدف أول 1.0R     189       41.3%     +0.05R   ✓
توسيع الوقف ≥0.50%            181       27.1%     −0.30R   ↓
الحالي (قبل التعديل)          191       19.9%     −0.26R
```

**الإعداد المطبَّق هو المتصدّر في كل المقاييس معاً:** أعلى نسبة ربح،
وأعلى توقّع، وأكثر صفقات محسومة، وصفر استبعاد، وثبات بين النصفين، وصفر
غموض داخل الشمعة. ولم يتفوّق عليه أي من الثمانية الأخرى.

**وأرقامه أفضل مما أظهره تشغيل 16/09** (`38.7%` و`+0.02R`) لأن المعادن
كانت تُقاس بالآجل حينها فتُحسم إشاراتها بأثر المرجع.

**أثره على الرموز التي صرّح صاحب المنتج بأهميتها:**

| الرمز | نسبة الربح قبل | بعد |
|---|---|---|
| XAGUSD | 17% | **47%** |
| XAUUSD | 30% | **64%** |
| XPTUSD | 14% | **62%** |
| ETHUSD | 8% | 33% |
| BTCUSD | 12% | 33% |

**تقاطع مستقل يعزّز النتيجة:** قياس `MFE` وجد أن وسيط ما يبلغه السوق
على الفضة **`0.96R`** — أي أن هدفاً عند `1.0R` يلتقط نحو النصف. والمحاكاة
أعطت **47%** بمنهج مختلف تماماً. رقمان من طريقين مستقلين يتقاطعان.

**أرضية الوقف مرفوضة نهائياً:** اختُبرت مرتين، بمرجع خاطئ ثم بمرجع
صحيح، وكانت أسوأ من الأساس في الحالتين (`−0.30R` مقابل `−0.26R`). حتى
مقترنةً بالهدف القريب تُضعف النتيجة (`+0.05R` مقابل `+0.15R`). والسبب
ثابت: توسيع الوقف مع هدف ثابت يكبّر المخاطرة بلا زيادة العائد.

**ملاحظة على الفضة:** تشخيص `MFE` أظهر أن 35% منها تموت بأول ربع ساعة،
واستُنتج منه أن الوقف يحتاج توسيعاً. **المحاكاة تنفي ذلك:** الهدف القريب
وحده يرفعها إلى 47% لأن نصف إشاراتها تبلغ `1R` قبل أن تُستوقَف. تشخيص
الحالة لا يكفي لاختيار العلاج — يلزم اختبار التدخّل نفسه.

---

### الفضة: المشكلة في **الوقف** لا في الاتجاه — والفريم 15m هو البؤرة
**الحالة:** `معلّق` — بانتظار محاكاة التدخّل بمرجع صحيح

**القياس** (34 قراراً على 26 يوماً، شموع `OANDA:XAGUSD` الفورية):

```
بلغت هدفها المُصدَر         5  (15%)
الوقف بأول ربع ساعة       12  (35%)   ← وقف داخل الضجيج
الاتجاه سليم والهدف بعيد    8  (24%)   ← يُصلحه تقريب الهدف
تحرّك ضعيف (0.5–1R)         4  (12%)
لم تتحرّك لصالحها إطلاقاً     5  (15%)   ← الاتجاه فعلاً خاطئ
```

**الاتجاه خاطئ في 15% فقط.** فقراءة السوق ليست المشكلة، والحكم بإيقاف
الرمز كان سيكون خطأً.

**والعلاقة بالفريم مطّردة بلا استثناء:**

| الفريم | عدد | وقف وسيط | لم تُعطَ فرصة |
|---|---|---|---|
| 15m | 16 | 0.263% | **56%** |
| 1h | 10 | 0.484% | 30% |
| 4h | 8 | 1.169% | **0%** |

كلما ضاق الوقف زادت الصفقات التي تموت قبل أن تُعطى فرصة — و**الفريم 4h
بوقف يتجاوز 1% لم تمت فيه ولا صفقة واحدة**. الاطّراد التام عبر ثلاث
شرائح يرجّح السببية على الارتباط.

**الهدف الأمثل للفضة `1.0R`** (توقّع `−0.12R`) — أي أن تعديل 16/09 هو
الأنسب لها أيضاً. **لكنه لا يكفي:** الفضة تبقى سالبة عند الهدف الأمثل،
لأن 35% تموت قبل بلوغ أي هدف. الهدف يعالج ربع المشكلة لا كلها.

**ما لا يُعرف بعد:** كم من الاثنتي عشرة صفقة الميتة كانت ستربح لو اتّسع
وقفها. لا يجيبه هذا القياس — `MFE` يتوقف عند لمس الوقف بحكم تعريفه.
يحتاج محاكاة تدخّل.

**قيد على المحاكاة القائمة:** `simulate_risk_configs.py` كان يجلب شموع
المعادن من `get_ohlcv` (آجل) — فنتائج الذهب والفضة بتشغيلَي 16/09 باطلة
بنفس سبب بطلان التشخيص الأول. أُصلح. وبقية الرموز سليمة لأن مستوياتها
من `get_ohlcv` نفسه، فاستنتاج «الهدف عند 1.0R» العام لم يتأثّر.

---

### كفيل: معرفة بالمنصة محلياً — والنبرة والحدود **متراجَع عنها**
**الحالة:** `مطبَّق` (معرفة المنصة) · `متراجَع عنه` (النبرة والحدود)

**سؤال صاحب المنتج كان عن الكلفة قبل الميزة** — «هل سيزيد الاستهلاك؟».
والبنية تسمح بالإجابة بلا مقايضة: التحليل لا يستدعي النموذج أصلاً
(المحرك المحلي يتولاه)، وأسئلة المنصة صارت تُجاب محلياً قبله. فالنموذج
لا يُستدعى إلا للشرح والأسئلة الجديدة فعلاً.

**ولماذا محلياً لا بالنموذج** — ثلاثة أسباب مرتّبة بالأهمية:
1. **الدقة قبل الكلفة:** النموذج لا يعرف باقاتنا فيؤلّف جواباً معقول
   الشكل خاطئ المضمون. والخطأ هنا يكلّف اشتراكاً.
2. **الكلفة:** أسئلة الدعم الأكثر تكراراً وأقلها تنوّعاً، فإجابتها
   محلياً تمنع ارتفاع الاستهلاك مع نمو المشتركين.
3. **السرعة.**

**الأسعار تُقرأ من `SiteSettings` لا تُكتب بالنص** — أُضيفت السنوية
والكوبونات بنفس الأسبوع، وأي رقم مكتوب كان سيتقادم صامتاً.

**توحيد الكتابة العربية قبل المطابقة** (همزات/تاء مربوطة/ألف
مقصورة/تشكيل): فشلت «ليش ما وصلتني» رغم وجود «ما وصلني» بالكلمات.
وملاحقة صيغ اللهجات بإضافة مرادفات لا تنتهي؛ توحيد الحرف مرة يغطي ما لا
تغطيه القوائم.

**قسما «الشخصية والنبرة» و«حدود لا تتجاوزها» أُضيفا ثم أُزيلا** بقرار
صاحب المنتج: `+376` توكن لكل استدعاء، ولا زيادة على الاستهلاك.

⚠️ **ما فُقد بالإزالة** — يُسجَّل لئلا يُفاجئ لاحقاً: لا يوجد الآن منع
صريح للنموذج من اختراع سعر باقة أو ميزة، ولا من الوعد بنسبة نجاح، ولا
من النصح بحجم صفقة. وهي ضوابط تخصّ **منتجاً مالياً** لا تحسيناً
تجميلياً.

**والمخاطرة مخفَّفة عملياً لا ملغاة:** أسئلة المنصة والأسعار تُجاب من
`platform_kb` محلياً فلا تصل النموذج أصلاً — وهي **أكثر مواضع الاختراع
احتمالاً** — والتحليل يُبنى بالمحرك المحلي لا بالنموذج.

**والطريق لاستعادتها بلا كلفة مفتوح:** تقسيم التوجيه إلى قاعدة قصيرة +
كتلة تنسيق التحليل تُرسَل فقط عند وجود `analysis_context`. فالشات العام
لا يحتاج أربعين سطراً من تنسيق الجداول، وإزالتها منه تفتح مساحة للضوابط
**مع خفض الإجمالي لا رفعه**.

**الكلفة المقاسة:** ~`$0.0015` للرسالة (توجيه 1096 توكن + آخر 8 رسائل +
800 حد أقصى للخرج) ⇒ **ألف رسالة يومياً ≈ `$43`/شهر كحدّ أقصى**، ودون
ذلك فعلياً لأن أسئلة الدعم والمفاهيم لا تمسّ الـAPI.

---

### رسالة الاستراتيجية: الاتجاه من المستويات لا من توصية المحرك
**الحالة:** `مطبَّق` — `strategy_engine.build_telegram_message`

**بلاغ حقيقي بمخرجات إنتاج:** رسائل تحمل `Direction: ⚪ WAIT` ومعها دخول
ووقف وهدف تصف صفقة صريحة. ونُفّذت اثنتان منها على حساب تجريبي **وبلغتا
الهدف** — فالمستخدم نفّذ صواباً **رغم** الرسالة لا بسببها.

**السبب:** `rec = analysis.get("recommendation")` — أي توصية المحرك، وهي
غالباً `WAIT` لأن للمحرك عتباته الخاصة. بينما الاستراتيجية أطلقت بشروط
**المستخدم**. فالرسالة تخلط قرارين مختلفين وتعرض أحدهما بلا الآخر.

**والتناقض ليس تجميلياً:** رسالة تحمل مستويات اتجاهية ولا تسمّي اتجاهها
تدفع المستخدم لتخمينه — وتخمينه خطأً **يعكس الصفقة**. وهي أخطر صيغة
غموض ممكنة في منتج تداول.

**القاعدة:** متى وُجدت مستويات فالاتجاه يُقرأ منها — الوقف تحت الدخول
شراء وفوقه بيع، بلا لبس. وتوصية المحرك تُذكر **بجانبها** متى خالفتها،
لأن إخفاء الاختلاف يوهم باتفاق لا وجود له.

**وغياب المستويات كان صامتاً:** تصل رسالتان متطابقتان لنفس الاستراتيجية
والرمز، إحداهما بمستويات والأخرى بلا، بلا ما يفسّر الفرق. والسبب أن
المحرك **يمسحها عمداً** حين ينحرف سعر التحليل عن السوق فوق الحد المسموح
(`rejection_reason` في `ai_engine_v5`) — وهو **رفض سلامة يجب أن يُقال لا
أن يُحذف بصمت**: الصمت يجعل المستخدم يظن الإعداد معطّلاً، أو يستنتج
مستويات من عنده.

**و`Confidence` أُعيدت تسميتها `Score (شروطك)`:** الرقم درجة الاستراتيجية
لا ثقة المحرك، وهما رقمان مختلفان تماماً كان الاسم يخلط بينهما.

---

### لوحة إرشاد دائمة تفحص البناء قبل الحفظ
**الحالة:** `مطبَّق` — `components/StrategyGuide.jsx`

الجولة التعريفية القائمة (`StrategyBuilderTour`) تُعرض **مرة واحدة ثم
تختفي للأبد**، وكانت تشرح الوزن والعتبة **دون ذكر أن الإطلاق يشترط منطق
المجموعات معهما** — أي أنها تُرسّخ نفس الفهم الناقص الذي أنتج البلاغ.
صُحّحت خطوتها، وأُضيفت لوحة **دائمة** بجانبها لا بديلاً عنها.

**الفرق الجوهري: اللوحة تفحص البناء الفعلي لا تشرح المفهوم فقط.** وأهم
فحص فيها **خطأ قاطع يُكتشف بالحساب لا بالانتظار**:

> مجموع أوزان الشروط دون العتبة ⇒ الاستراتيجية **لا يمكن** أن تُطلق مهما
> تحققت شروطها جميعاً.

مستخدم بشرطين وزنهما 36 وعتبة 70 كان سينتظر تنبيهاً **مستحيلاً** بلا أن
يعرف — وقد يستنتج أن المنصة لا تعمل. الآن يُقال له فوراً، مع الرقم الذي
يصلحه (`اخفض العتبة إلى 31 أو أقل`).

وفحوص أخرى مُختبَرة: مجموعة «الكل» بخمسة شروط (ممكنة لكن نادرة الوقوع)،
ومجموعة تطلب «4 من 2» (مستحيلة)، وشروط خارج المجموعات (تُحتسب بالدرجة
ولا تدخل منطق الإطلاق).

**ونصائح المبتدئ مكتوبة بعكس الحدس عمداً:** «ابدأ بثلاثة شروط، ستُطلق
كثيراً — وهذا مقصود». لأن ميل المبتدئ إلى التشديد المبكر يُنتج استراتيجية
صامتة شهراً، فيظنّها معطلة. والتحذير من ذلك أنفع من شرح الميزات.

---

### باني الاستراتيجيات يعرض الدرجة ويخفي شرطها
**الحالة:** `مطبَّق` — `strategy_engine.evaluate_strategy` + عمود
`diagnostics_json`

**بلاغ حقيقي:** عتبة مضبوطة على `50`، وسجلّ المراقبة يعرض `Score 54`
مراراً بلا إطلاق. فبدا النظام معطلاً.

**وهو يعمل بالضبط كما عُرّف.** الإطلاق يشترط **أمرين**:

```python
triggered = groups_passed and score >= min_score and len(enabled) > 0
```

والسجل كان يعرض **الثاني وحده**. فحين يتحقق المعروض ولا يقع الإطلاق،
لا يملك المستخدم إلا أن يستنتج عطلاً. **عرض الرقم دون شرطه إخفاءٌ بصيغة
إظهار** — وهو أسوأ من عدم العرض، لأنه يمنح ثقة في فهم ناقص.

**والسبب لا يُستنتج بأثر رجعي:** حالة السوق وقت التقييم غير متاحة لاحقاً.
فيلزم حسابه وتخزينه **لحظتها** — ومن هنا العمود الجديد لا مجرد تحسين عرض.

**الصيغة المعروضة الآن** (مُختبَرة على حالة البلاغ):

> ⚠ الدرجة كافية (60 ≥ 50) لكن منطق المجموعات لم يكتمل:
> مدرسة ICT (الكل) — تحقّق 2 من 3، ينقص: FVG

تسمّي المجموعة، ومنطقها، وكم تحقّق منها، **وأي شرط بعينه ينقص**.

**الأحداث السابقة تبقى بلا سبب** — لا يُختلق لها تفسير.

---

### تشخيص الفضة الأول — **باطل**، قِيس بالعقود الآجلة
**الحالة:** `متراجَع عنه` — أُصلحت الأداة وتُعاد القراءة

`diagnose_symbol_excursion.py` جلب شموع الفضة من `get_ohlcv` أي `SI=F`
**الآجل**، بينما مستويات الفضة **فورية** (يزيحها `_apply_spot_basis`).
وهو العطل نفسه الموثّق يوم 16/09 — ارتكبتُه في أداة جديدة بعد يوم من
تصحيحه في أداتين.

**حجم الأثر يجعل النتيجة بلا معنى لا ناقصة الدقة فقط:** الـbasis وقتها
`0.96$` بينما مسافة وقف الفضة `0.07–0.14$`:

```
#2508  الوقف 0.06964  →  الفارق 13.8× الوقف
#2509  الوقف 0.09768  →  الفارق  9.8×
#2490  الوقف 0.12142  →  الفارق  7.9×
```

فكل إشارة بيع تبدو ملموسة الوقف فور إصدارها. وقد ظهر ذلك حرفياً: **خمس
إشارات بيع من خمس بـ`MFE = 0.00R` بالضبط** — وهو أثر إزاحة المرجع لا
حركة السوق. والخلاصة التي بُنيت عليه («60% اتجاهها خاطئ») باطلة.

**عيب ثانٍ بنفس الأداة:** الحكم كان يقول «الاتجاه خاطئ» لكل `MFE` منخفض،
خالطاً حالتين: الوقف الملموس بأول شمعة يعني أن الصفقة **لم تُعطَ فرصة**
(وقفها داخل ضجيج الدقائق الأولى)، لا أن قراءة الاتجاه خاطئة. والقراران
متعاكسان: الأول يُصلَح بتوسيع الوقف والثاني بمراجعة التحليل — والخلط
بينهما يقود لإيقاف رمز سليم الاتجاه.

**القاعدة المستخلَصة:** أي أداة تقرأ شموعاً لمقارنتها بمستويات إشارة
**تسأل أولاً عن مرجع تلك المستويات**. المعادن فورية دائماً ⇒
`fetch_tv_history` لا `get_ohlcv`، وإن تعذّرت فالامتناع عن الحكم لا
السقوط للآجل.

---

## 2026-09-16

### `await` على دالة متزامنة أوقف الرصد التلقائي — ومصدر السعر فُصل عن مصدر الشموع
**الحالة:** `مطبَّق` — `app/api/bot.py`

`get_realtime_price_with_meta` **متزامنة** وتُعيد `dict`. وكل مواضع استدعائها
بالمشروع (`markets.py`, `main.py`, `ai_engine_v5`, `chat_agent`, `smart_data`)
تستدعيها بلا `await` — **عدا موضعين داخل `bot_check_outcomes` وحدهما**. وعليهما
كان `await dict` يرمي `TypeError` يلتقطه `except` الحلقة فتُتخطّى الإشارة.

**الأثر:** الرصد التلقائي ميت لكل رمز يمرّ من ذلك المسار. كان محصوراً بالرموز
خارج TradingView (`ADAUSD`, `DOGEUSD`, `XPTUSD`, `COPPER`, `NATGAS`, `NFLX`،
الأسهم الخليجية) فظلّ مخفياً لقلّتها — إشارات من 14/09 و15/09 بقيت `ACTIVE`
بلا أن ينتبه أحد. ثم عمّ **كل الرموز** بتغيير 16/09، فظهر فجأة كـ«كل الإشارات
عالقة».

**لماذا لم يُكتشف مبكراً:** `except` الحلقة كان **يسجّل** التحذير فعلاً
(`logger.warning(f"check_outcomes signal {sig.id}: ...")`). فالخلل لم يكن في
غياب السجل بل في أن **أحداً لم يقرأه** — وسجلات الحاويات تُمحى مع كل إعادة
إنشاء. الدرس: تسجيل التحذير لا يكفي حين لا توجد آلية تنبيه.

**وفُصلت قاعدتان كانتا مدمجتين في `_SPOT_SYMBOLS` واحدة:**
- **مرجع شموع المشي** ← يجب أن يطابق مرجع بناء المستويات ⇒ المعادن فقط.
- **مصدر السعر اللحظي** ← يُختار بالإتاحة والكلفة ⇒ كاش TV لكل رمز متاح،
  لأنه مجاني ومحدَّث بينما أي مسار آخر طلبُ شبكة لكل رمز كل 90 ثانية.

دمجهما في متغير واحد جعل تصحيح المرجع ينقل السعر اللحظي قسراً لمسار لم يُقصد.

---

### الهدف الأول عند 1.0R والثاني عند 2.0R بدل مسافة ثابتة بالـATR
**الحالة:** `مطبَّق` — `ict_engine.calculate_levels` · **قرار صاحب المنتج
بتفويض صريح (16/09)**

**الهدف:** رفع نسبة الإشارات الرابحة. `20.2%` لا تُباع مهما كان التوقّع.

**التغيير:** `tp1 = entry ± risk × 1.0` و`tp2 = entry ± risk × 2.0`، حيث
`risk = |entry − sl|`. **الوقف لا يُمسّ إطلاقاً.**

**الدليل** (`simulate_risk_configs.py`، 210 قرارات، شموع 15m، الحكم من
شموع السوق لا من نتائجنا المسجّلة):

```
                    مُتداوَلة  نسبة الربح   التوقّع    النصف الأول ← الثاني
الحالي                 173       20.2%     −0.22R      6% ← 23%
هدف أول 1.0R           191       38.7%     +0.02R     29% ← 41%
```

**التحسّن صامد في نظامَي سوق:** `+23` نقطة بالنصف الأول و`+18` بالثاني.
ولاحظ أن الأساس نفسه قفز `6% → 23%` — أي أن النافذة شهدت تحوّلاً سوقياً
أصاب كل الإعدادات، والمعوَّل عليه **الفرق عن الأساس داخل كل نصف** لا
المستوى المطلق. (المقياس الأول بالأداة قاس ثبات كل إعداد وحده، وهو الشيء
الخطأ.)

**الآلية سببية لا إحصائية:** هدف على بُعد مسافة الوقف أسهل بلوغاً من هدف
على بُعد 2.5 ضعفها. فالنتيجة ليست نمطاً منتقى من ضجيج.

**لماذا رُفض الإعداد الأعلى نسبةً** (`وقف ≥0.50% + هدف 1.0R`، 41.0%):
يفرض وقفاً بـ0.50% على أزواج الفوركس — أي المخاطرة بحركة يوم كامل بصفقة
واحدة. فلا تُحسم داخل أفقها وتسقط من الحساب: `EURGBP 4→0`, `EURUSD 2→0`,
`GBPJPY 3→0`, `DXY 2→0`. **نسبته الأعلى حذفٌ للحالات الصعبة من المقام لا
تحسين.**

**أثر جانبي معلَن:** `rr = |tp2−entry|/risk` يصير **ثابتاً عند 2.0** لكل
إشارة، فيتعطّل فلتر الحد الأدنى لـR/R عملياً. مقبول لأن شريحة
`R/R 1.5–2.0` كانت **غير مستقرة** بتقرير الجودة — الفلتر لم يكن يميّز
شيئاً. ومستوى السيولة (`bsl`/`ssl`) يُحفظ في `tp3` فلا تُفقد معلومة ICT.

**قيود الدليل:** العينة ~6 أيام (yfinance يفرض مدى ثابتاً فلم تتّسع
النافذة بشموع 15m)، وبلا سبريد ولا انزلاق، والتوقّع `+0.02R` بالكاد فوق
الصفر — المكسب في **نسبة الربح** لا في الربحية.

**شرط الحكم — كُتب قبل التطبيق عمداً:**
بعد **7 أيام** من بيانات مقيسة بحلقة رصد سليمة (بند «قبل إصلاح حلقة
الرصد» = 0%)، على عينة **≥ 40 قراراً مغلقاً**:

```
نسبة الربح ≥ 33%          →  يُبقى
نسبة الربح < 28%          →  يُتراجَع عنه فوراً
بينهما                    →  تُمدَّد الملاحظة أسبوعاً ولا يُبنى عليه
```

كُتب مسبقاً لئلا تُبرَّر النتيجة بعد وقوعها أياً كانت. والتراجع سطران.

---

### توسيع الوقف لا يفيد — والإصلاح هو تقريب الهدف الأول
**الحالة:** `مطبَّق` — الشقّ الأول **مرفوض نهائياً**، والثاني نُفّذ وتأكّد (راجع 17/09)

**استنتاج سابق أُبطل:** قيل إن الوقف الضيق (35% من الإشارات دون 0.15%) هو
المشكلة المؤكدة، استناداً إلى أن شريحة `0.50–1.00%` توقّعها `+0.68R` بتقرير
الجودة. **المحاكاة تنفيه:** فرض أرضية وقف يجعل الأداء **أسوأ** من الحالي.

```
الحالي (بلا تغيير)        158 صفقة   21.5%   −0.16R
توسيع الوقف ≥0.30%        152 صفقة   24.3%   −0.21R  ↓
توسيع الوقف ≥0.50%        143 صفقة   28.7%   −0.18R  ↓
هدف أول عند 1.0R فقط      174 صفقة   41.4%   +0.10R  ★
```

**سبب البطلان — ارتباط لا سببية:** شريحة الوقف الواسع كانت **انتقاءً**
(إشارات وُلدت هكذا في ظروف سوق معيّنة)، لا **تدخّلاً**. وحين يُفرض الوقف
الواسع على إشارات أخرى لا ينتقل الأثر: المخاطرة تكبر والعائد ثابت، فكل ربح
يساوي `R` أقل وكل خسارة تبقى `−1R`.

**الدرس المعمَّم:** الشريحة التي تبدو ممتازة بالتحليل الوصفي قد تكون صفة
للإشارات التي وقعت فيها لا للشرط نفسه. **لا يُطبَّق شرط على المحرك قبل
محاكاته كتدخّل.** ولولا المحاكاة لطُبّقت أرضية وقف تجعل الأداء أسوأ وتبدو
مدعومة بالبيانات.

**البديل الذي نجح:** `tp1 = entry ± risk × 1.0` و`tp2 = ±risk × 2.0` بدل
مسافة ثابتة بالـATR. لا يمسّ الوقف، ولا يستبعد إشارة واحدة (التغطية
محفوظة)، ويضاعف نسبة الربح تقريباً.

**أثر جانبي يجب إعلانه:** `rr = |tp2−entry|/risk` سيصير **ثابتاً عند 2.0**
لكل إشارة، فيتعطّل فلتر الحد الأدنى لـR/R عملياً. مقبول لأن شريحة
`R/R 1.5–2.0` كانت **غير مستقرة** بتقرير الجودة (انقلبت بين النصفين) — أي
أن الفلتر لم يكن يميّز شيئاً حقيقياً. لكنه تغيير في بنية القرار لا في قياسه.

**ما يمنع التطبيق الآن:** التشغيل الأول غطّى **193 من 437** قراراً على
**~6 أيام فقط** (شموع 5m لا تعود أبعد) — أي **نظام سوق واحد**. والهدف
القريب يتفوّق في سوق متذبذب وقد يتراجع في سوق مُتجه. أُضيف للأداة: شموع
15m (تصل ~50 يوماً)، وفحص ثبات بنصفَي الفترة لكل إعداد، وعدّاد الغموض داخل
الشمعة.

---

### أرضية دنيا لمسافة الوقف
**الحالة:** `معلّق` — بانتظار قياس الأثر وقرار صاحب المنتج

**المشكلة المقاسة:** 9 من 26 إشارة (35%) مسافة وقفها **دون 0.15%** —
`EURUSD 0.027%` (≈3 نقاط، أضيق من فرق السعر عند الوسيط)، `DXY 0.036%`،
`BTCUSD 0.085%`. وقف داخل نطاق الضجيج يُضرب بشبه يقين بلا علاقة بصحة
التحليل. ويؤكده تقرير الجودة: مسافة `0.50–1.00%` توقّعها `+0.68R` (ثابتة
في نصفَي الفترة) بينما ما دون `0.30%` يقارب الصفر أو يسلب.

**السبب في المحرك:** `ict_engine.calculate_levels` يأخذ **أقرب** swing
(`max(valid_lows) - atr*0.5` للشراء) **بلا حدّ أدنى للمسافة**. وفي سوق
هادئ يقع ذلك القاع على بُعد نقاط معدودة.

**الأثر الجانبي الذي يمنع التنفيذ المباشر:** توسيع الوقف يخفض
`rr = |tp2-entry| / |entry-sl|`، والمحرك يرفض ما دون حدّ أدنى لـR/R —
فالأرضية ستُسقط إشارات كانت تمرّ. وهذا يصطدم بمطلب صريح: تغطية واسعة
للذهب والفضة والنفط والناسداك. **حجم التقليص غير معلوم، ولا يُخمَّن.**

**الهدف المعلن من صاحب المنتج (16/09): رفع نسبة الإشارات الرابحة**، لا
التوقّع وحده — «نسبة ربح 29.5% لا تُباع مهما كان التوقّع موجباً». وهذا
يعيد تعريف المسألة: النظام يعمل بنمط *قليل الربح كبير العائد*
(`R/R ≈ 2.5`, تعادل عند 28.6%) وهو أصعب نمط تسويقياً. والانتقال إلى
*كثير الربح معتدل العائد* يخدم الهدفين معاً بحسب البيانات المتاحة:
`0.45 × 1.5 − 0.55 = +0.13` مقابل `0.295 × 2.5 − 0.705 = +0.03`.

**الأداة:** `simulate_risk_configs.py` — قراءة محضة. تأخذ **مدخلات**
الإشارات التاريخية فقط (الوقت/الرمز/الاتجاه/الدخول، وكلها مكتوبة لحظة
الإصدار فلا تمسّها أعطال الرصد)، وتعيد بناء المستويات بكل إعداد، ثم تمشي
على **شموع السوق الحقيقية**. فلا تعتمد على أي نتيجة مسجّلة إطلاقاً.

قُيس الحدّ الأدنى **بنسبة مئوية** لا بمضاعف ATR: النسبة هي ما أظهرته
البيانات فعلاً (`0.50–1.00%` ⇒ `+0.68R`)، وهي مباشرة القراءة وقابلة
للتحويل إلى قاعدة يفهمها غير المبرمج.

وتُقاس صيغتان مختلفتا الأثر: **توسيع** الوقف للحد الأدنى (يحفظ التغطية)
مقابل **استبعاد** الإشارة كلياً (يقلّصها).

---

### كل البيانات قبل 2026-09-16 غير صالحة لاستنتاج استراتيجي
**الحالة:** `مطبَّق` — `OUTCOME_FIX_DATE` في `app/services/quality_report.py`

حلقة رصد النتائج حملت أربعة أعطال أُصلحت كلها في هذا اليوم. **أثر أحدها
غير محايد للاتجاه**، وهو ما يجعل البيانات القديمة مضلِّلة لا مجرد صاخبة:
بسقوط المشي الزمني كان الحكم يعود إلى «أين السعر الآن» **بلا تحديد زمني**،
فيكفي أن يهبط السعر تحت وقف صفقة شراء لحظةً خلال أيام لتُسجَّل خسارة. وفي
سوق هابط تُصفّى صفقات الشراء كلها تقريباً بينما يبلغ البيع أهدافه.

**البصمة العددية:** خمس توليفات بنسبة ربح **0.0% بالضبط**، إحداها على 40
قراراً عبر 9 رموز. احتمال ذلك لو كانت النسبة الحقيقية 25% ≈ **1 من 100,000**.

**ما يترتب:** لا يُبنى قرار عن الاتجاه أو الرموز أو عتبات الثقة على بيانات
تسبق هذا التاريخ. يُقرأ بند «قرارات مسجَّلة قبل إصلاح حلقة الرصد» أول تقرير
الجودة قبل أي شيء آخر.

---

### استنتاج «الشراء خاسر بنيوياً» — **مُبطَل**
**الحالة:** `متراجَع عنه`

قيس على نافذتَي 30 و90 يوماً: `BUY −0.38R / 17.2%` مقابل `SELL +0.30R`،
والفجوة `0.67–0.68R` **ثابتة عبر النافذتين** و34 رمزاً وأربع فترات فرعية.
واستُنتج منه أنها بنيوية لا حالة سوق.

**سبب البطلان:** الحجّة كانت «الفجوة ثابتة عبر نافذتين مستقلتين» — والنافذتان
تشتركان في **المصدر المعطوب نفسه** (كل البيانات تسبق 16/09). وفحص الثبات
بنصفَي الفترة لا يكشف هذا النوع إطلاقاً: **نصفا أي نافذة قديمة كلاهما قبل
تاريخ الإصلاح**، فكان يؤكد اتساق بيانات معطوبة لا صحتها.

**الدرس المعمَّم:** ثبات رقم عبر نافذتين لا يثبت شيئاً إن كانتا تقيسان بالأداة
المعطوبة نفسها. تُفحص **سلامة المصدر** قبل ثبات النتيجة.

---

### النقاط ليست عملة موحّدة — الحكم بمضاعف R
**الحالة:** `مطبَّق` — `_r_multiple` في `app/services/quality_report.py`

`_calc_points` يعطي مضاعفاً مختلفاً لكل فئة أصول، فحركة 1% تساوي ~360 نقطة
على الذهب و~200 على ناسداك و~100 على الكريبتو و~1.5 على سهم أمريكي و~0.3
على الغاز — **فارق يبلغ 1200 ضعفاً**. كل مضاعف دفاعي وحده (يطابق تسعير
سوقه)، فليس عطلاً يُصلَح — لكن **أي مجموع نقاط عابر للرموز متوسط مرجّح
بأوزان اعتباطية**، وضبط الاستراتيجية عليه يضبطها على المضاعف لا على الأداء.

**انقلابات حقيقية مقاسة:** `XAUUSD +512.78` نقطة و`−0.07R` (متصدّر جدول
النقاط، وخاسر فعلياً). `BUY +670.02` نقطة و`−22.3R`. `XAUUSD −6703` مقابل
`XAGUSD −59` على عددَي قرارات متقاربين — النقاط تقول 113 ضعفاً، وR يقول ضعفين.

**القاعدة:** كل حكم يُبنى على التوقّع (متوسط R). النقاط تبقى معروضة كلغة
تقارير معتادة، لا كأساس قرار.

---

### قياس النتيجة بنفس مرجع بناء المستويات
**الحالة:** `مطبَّق` — `_SPOT_SYMBOLS = {"XAUUSD","XAGUSD"}` في `app/api/bot.py`
و`_SPOT_ADJUSTED` في `app/api/admin.py`

السؤال «هل بلغ السعر المستوى؟» بلا معنى إن كان المستوى من مصدر والسعر من
مصدر آخر. **المعادن** مستوياتها تُزاح للفوري بـ`_apply_spot_basis` فمرجعها
شموع TV الفورية؛ **ما عداها** مستوياتها من `get_ohlcv` فمرجعها `get_ohlcv`.

**الشاهد:** `EURUSD #2528` — مسافة وقفها 2.4 نقطة بينما فارق
`yfinance EURUSD=X` عن `OANDA:EURUSD` ~1.8 نقطة. فاختلاف المصدرين وحده، بلا
أي حركة سوق، كفى ليُسجَّل `TP2_HIT` خلال دقيقة من الإصدار.

**تراجع جزئي عن قرار 2026-09-10** الذي وسّع شموع TV لكل الرموز بعد بلاغ
`USOIL` (سُجّل هدف بينما سعر حساب Exness كان تحت الوقف). التوسيع عالج انحراف
مصدر السعر لكنه أنشأ خللاً أشدّ: صار الحكم يُقرأ بمسطرة غير التي رُسم بها
المستوى.

**الثمن المقبول صراحةً:** قد تعود حالات تخالف فيها النتيجة ما يظهر على منصة
الوسيط. **الجذر الحقيقي** أن المستويات نفسها تُبنى من `yfinance`، ولا يُعالَج
إلا بتغيير مصدر التوليد — وهو يمسّ المحرك ومؤجَّل عمداً. التوسيع كان يضيف
خطأ ثانياً فوق الأول لا يرفعه.

---

### حكم المشي الزمني نهائي **فقط** متى غطّى عمر الإشارة
**الحالة:** `مطبَّق` — `walked` في `app/api/bot.py`

منع السعر اللحظي (مصدر مختلف) من نقض مشي زمني ناجح. لكن الصيغة الأولى
`walked = bool(candles)` كانت خاطئة بنيوياً وأوقفت إغلاق الإشارات بالكامل
لساعات: **نافذة المشي 30 شمعة 5m = ساعتان ونصف، وعمر الإشارة يبلغ 72 ساعة**.
فإشارة لمست وقفها قبل ثلاث ساعات لا تظهر لمستها بالنافذة، فيخلص المشي إلى
«لم يُلمس شيء» ويُلغى الفحص اللحظي — فتبقى نشطة إلى الأبد.

**الشرط الصحيح:** `earliest_ts <= created_ts` — أي توفّرت شمعة عند لحظة
الإنشاء أو قبلها، وتُلتقط **قبل** فلترة `created_ts` لا بعدها. وإلا يبقى
الفحص اللحظي شبكة الأمان.

**الدرس المعمَّم:** الفرق بين «هل توجد بيانات؟» و«هل غطّت البيانات المدى
المطلوب؟» هو الفرق بين حكم مكتمل وحكم على شريحة.

---

### حدّ صلب: التحقق التاريخي لا يتجاوز ~3.5 أيام
**الحالة:** `مطبَّق` — قيد بنيوي لا خيار

`_verify_signal_outcome_core` تجلب 1000 شمعة 5m ≈ 3.5 أيام، وفيها حارس صريح
يعيد `NO_DATA` إن كانت أقدم شمعة متاحة بعد لحظة الإنشاء. **فتوسيع نافذة
التصحيح لا يوسّع التغطية الفعلية** — الصفوف الأقدم تُستبعد بالامتناع لا
بالتخمين، وهو السلوك الصحيح: الحكم بتلك الشموع كان سيقيس نافذة زمنية أخرى
تماماً لا حياة الإشارة.

**ما يترتب:** الماضي البعيد **غير قابل للتصحيح** بدقة الخمس دقائق. البدائل
(شموع ساعة تصل ~42 يوماً) تُضيّع الترتيب داخل الشمعة فيكثر الغموض بين الوقف
والهدف. `fix_outcome_discrepancies.py --days N` يطبع التغطية الفعلية.

---

### الباقة السنوية تُخزَّن كـ`monthly` على المستخدم
**الحالة:** `مطبَّق` — `app/models/payment.py` + `app/services/subscription_service.py`

أُضيفت `PaymentPlan.YEARLY` فقط، وبقي `users.plan = MONTHLY` بمدة 365 يوماً.

**السبب:** `PlanType.WEEKLY/MONTHLY` مكتوبان **يدوياً** كـ«الباقات المدفوعة»
في ~25 موضعاً بثمانية ملفات — فلتر مشتركي البوت (`bot.py:369`)، صلاحية
الإشارات (`signals.py:71`)، حدود الشات (`chat.py:42`)، `auth.py`،
`affiliate.py`، إحصاءات `admin.py`. فإضافة `PlanType.YEARLY` كانت ستُسقط
المشترك السنوي **صامتاً** من كل تلك الفحوص: يدفع 179.9$ ولا تصله إشارة. ولا
يظهر الخلل في اختبار، بل في شكوى عميل.

**الثمن:** شارة الباقة تقول «شهري» لمشترك سنوي (تاريخ الانتهاء صحيح).
**الإصلاح الصحيح** توحيد الفحوص في ثابت `PAID_PLANS` واحد — تعديل مستقل لا
يُخلط بإطلاق باقة.

---

### قواعد الكوبونات
**الحالة:** `مطبَّق` — `app/services/coupon_service.py`

- **الخصم يُحسب على الخادم دائماً.** الواجهة ترسل الرمز لا المبلغ، وكل مسار
  دفع (USDT / Stripe Checkout / Stripe Elements / Spaceremit) يمرّ بنقطة
  تسعير واحدة. أي مسار يحسب بنفسه هو الذي سيُنسى عند تعديل القواعد فيبيع
  بسعر خاطئ — وخلل الدفع يظهر بالفاتورة لا بالاختبار.
- **العدّاد يزيد عند اعتماد الدفعة لا إنشائها** — وإلا استُنفد كوبون محدود
  بدفعات معلّقة لا تُدفع أبداً. ويقابله تجاوز مؤقت للحد بدفعات متزامنة، وهو
  احتمال مقبول أمام ضرر مؤكد.
- **`amount_usd` = ما حُصّل فعلاً** لا سعر الباقة — وإلا ضُخّم الإيراد
  و**حُسبت عمولة المسوّق على مبلغ لم يُقبض**.
- **كوبون استُخدم يُوقَف ولا يُحذف** — الحذف يُسقط سجل الاستخدامات
  (`CASCADE`) فتضيع محاسبة خصومات مُنحت فعلاً.

---

### حرّاس تقرير الجودة — ولماذا كلٌّ منها
**الحالة:** `مطبَّق` — `app/services/quality_report.py`

كل حارس نشأ من خطأ وقع فعلاً، لا من اعتبار نظري:

| الحارس | نشأ من |
|---|---|
| عينة ≥ 10 | اقتراحات مبنية على 3–4 قرارات تبدو قاطعة |
| هيمنة رمز واحد ≤ 60% | تحليل مسافة وقف كاد يُفقد رمزاً رابحاً — الحكم وقع على الرمز لا الرافعة |
| هيمنة رمزين ≤ 75% | `hhi` كان يُحسب **ولا يُستخدم**؛ شريحة 55%+30% كانت تمرّ سليمة |
| تداخل الفرضيات بـ`lift` لا نسبة خام | شريحة تشكّل 53% تتقاطع مع أي شريحة بـ~53% صدفةً — الإنذار الكاذب يدفع لتجاهل الحقيقي |
| ثبات النصفين بثلاث حالات | وسم «انقلبت» و«تفاوتت شدتها» بوسم واحد أنتج نصيحة ضارة: حُجبت فرضية صحيحة بحجة منهجية تبدو رصينة |

**مبدأ عام:** الشريحة المرفوضة **تُعرض موسومة ولا تُخفى**. إخفاؤها يوحي
بشمول التحليل، وعرضها بلا وسم يدعو لقرار خاطئ.

---

### `killzone` — ثالث تكرار لنفس صنف العطل
**الحالة:** `مطبَّق` — `app/api/bot.py`

عمود موجود بالجدول ولم تُكتب فيه قيمة **قط** (تغطية 0% على 281 قراراً)،
بينما المحرك يحسب الجلسة فعلاً في `get_kill_zone` **ويبني عليها الثقة**.
كنا نحلّل أثر رافعة على عمود فارغ.

نفس ما أصاب `wyckoff_phase` و`premium_discount` (15/09): **الحقل موجود،
والمصدر موجود، والربط بينهما مفقود** — والقيمة تحت مفتاح متداخل
(`analysis["kill_zone"]["active_session"]`) لا في جذر التحليل.

**قاعدة وقائية:** عند إضافة عمود تحليلي جديد، يُفحص أول امتلائه على بيانات
إنتاج حقيقية — لا يكفي أن الكود يمرّ.

---

## قرارات سابقة ما زالت سارية

### `_WINRATE_OVERRIDE = 0.50` لا يُرفع قبل عينة موثوقة
**الحالة:** `مطبَّق` — `app/services/ai_engine_v5.py:970`

`get_winrate()` تُعيد قيمة ثابتة محايدة بدل أي رقم تاريخي، فلا تصل نتائج
قاعدة البيانات إلى معايرة المحرك. **شرط الرفع:** 50+ قراراً محسوباً من
**2026-09-16** فصاعداً (كان W35 قبل اكتشاف أعطال حلقة الرصد).

### `decision_grouping` هو المصدر الوحيد لكل تجميع
**الحالة:** `مطبَّق` — `app/services/decision_grouping.py`

القرار الواحد يُحفظ صفاً لكل مستخدم استلمه، فالعدّ الخام يضخّم كل شيء
بمعامل عدد المستخدمين. كل تقرير ومؤشر يمرّ بـ`verified_unique_decisions()`.

### محرك ICT هو المصدر الوحيد للاتجاه والمستويات
**الحالة:** `مطبَّق`

Gemini يُستخدم **لإثراء النص فقط**. أي خلل في Gemini (429/404) لا أثر له
على نسبة الربح أو جودة الإشارة.

### لا يُشغَّل Docker nginx
**الحالة:** `مطبَّق`

`system nginx` على السيرفر يتولى المنفذ 80/443 وشهادات SSL.
