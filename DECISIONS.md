# سجل القرارات — Mosh AI Pro v5 / Qaffel AI

> **ما هذا الملف:** القرارات التي لا تُستنتج من الكود. الكود يقول *ماذا* يفعل
> النظام، وسجل git يقول *متى* تغيّر — وكلاهما لا يقول **لماذا اختير هذا دون
> غيره، وما البديل الذي رُفض ولماذا**. تلك المعرفة تضيع بانتهاء الجلسة، فيُعاد
> فتح نقاش محسوم أو — أسوأ — يُنقض قرار لأسبابه ما زالت قائمة.
>
> **يُقرأ قبل أي تعديل** على المحرك أو حلقة الرصد أو المدفوعات أو التقارير.
>
> **هذا ملخّص.** التفصيل الكامل (الأرقام، الجداول، سبب كل بطلان) في
> [`DECISIONS_ARCHIVE.md`](DECISIONS_ARCHIVE.md) — يُقرأ فقط عند الحاجة لتفصيل
> بعينه، لا كل جلسة. لا يُكتب بالأرشيف مباشرة؛ كل قرار يُولد هنا ثم يُنقل حين
> يصبح تاريخياً محضاً.

---

## قاعدة التحديث

يُضاف قيد **في نفس اللحظة** التي يُتخذ فيها القرار، لا في نهاية العمل، متى
تحقّق أحد هذه:

- تعديل يمسّ **توليد الإشارات** أو **قياس نتائجها**
- قرار يتعلق بـ**المدفوعات** أو الباقات أو الصلاحيات
- **تراجع** عن قرار سابق — يُوسم الأصل `متراجَع عنه` ولا يُحذف
- **استنتاج أُبطل بالقياس** — الاستنتاج الخاطئ يُسجَّل مع سبب بطلانه، وإلا
  أُعيد اكتشافه لاحقاً وبُني عليه من جديد
- **قيد تقني** يحدّ ما يمكن عمله (نافذة بيانات، حدّ API، بنية قديمة)

**لا يُسجَّل هنا:** ما يقوله الكود بوضوح، ولا إصلاحات الأخطاء الروتينية،
ولا تفاصيل تخصّ جلسة واحدة.

**الصيغة:** الأحدث أولاً. كل قيد يحمل حالته: `مطبَّق` / `معلّق` / `متراجَع عنه`.
قيد جديد يُكتب هنا **بتفصيله الكامل** وقت اتخاذه (لا كسطر واحد) — التلخيص
يقع لاحقاً حين ينتقل للأرشيف.

---

## 2026-09-22

### تسجيل الدخول بحساب Google — password_hash صار nullable عمداً
**الحالة:** `مطبَّق` (الباك-إند + الفرونت-إند). **`معلّق`**: تشغيل الهجرة
ورفع ملف اعتماد Firebase على السيرفر — لم يحصلا بعد.

**قيد تقني يُقرأ قبل أي لمسة لموديل `User` مستقبلاً:** `password_hash`
كان `nullable=False` دائماً، وصار `nullable=True` بقصد — حسابات Google
(`auth_provider="google"`) **ما عندها كلمة سر إطلاقاً**، التحقق يصير عبر
Firebase ID token لا `verify_password`. لو رجّعت العمود `NOT NULL` بدون
مراجعة هالسبب، أي حساب Google موجود ينكسر تسجيله بصمت.

**التصميم:** حساب واحد بالبريد — لو مستخدم كلمة سر موجود سجّل بقوقل بنفس
البريد، يُربط الحساب الموجود (`auth_provider` الأصلي يبقى كما هو، فقط
`is_verified` يتفعّل لو قوقل وثّق البريد) بدل إنشاء حساب مكرَّر.
حسابات Google الجديدة تمرّ بنفس مسار حساب التجربة (`_provision_trial_user`،
مستخرَج من `/register` ليُستخدَم بالاثنين معاً) — نفس الباقة، النقاط،
تنبيه الأدمن، إيميل الترحيب — عدا رمز تفعيل البريد (غير لازم، قوقل وثّقه).

**قبل ما يشتغل فعلياً على الإنتاج (خطوات المنتج لا الكود):**
1. تشغيل `backend/migrate_add_google_auth.py` على قاعدة بيانات الإنتاج.
2. توليد ورفع ملف اعتماد حساب خدمة Firebase (Service Account JSON) على
   السيرفر، وضبط `FIREBASE_CREDENTIALS_PATH` بـ`.env.prod` — بدونه
   `/api/v1/auth/google` يرجّع 503 بوضوح (لا يفشل بصمت).
3. إضافة نطاق الإنتاج الحقيقي (`qaffel.com` أو ما يعادله) بـFirebase
   Console → Authentication → Settings → **Authorized domains** — بدونها
   `signInWithPopup` يفشل بـ`auth/unauthorized-domain` بالإنتاج فقط
   (يعمل محلياً على `localhost` لأنه مُصرَّح افتراضياً).

**أثر جانبي مقصود لا خطر منه:** حزمة الفرونت-إند الرئيسية زادت
~100KB (Firebase Auth SDK) لأن `AuthContext.jsx` يستورد `firebase.js`
مباشرة — لم يُقسَّم بتحميل كسول، قرار بسيط لتفادي تعقيد إضافي بميزة
تسجيل دخول أساسية يحتاجها كل مستخدم على أي حال.

---

### RR المعروض للمستخدم كان أقل من الحقيقي — Rule 10 يوسّع الهدف ولا يحدّث RR
**الحالة:** `مطبَّق` — استثناء ثانٍ من تجميد 17/09، بإذن صريح من صاحب المنتج

**بلاغ حقيقي بدأه المستخدم بالصدفة** أثناء تدقيق إشارة BNBUSD (BUY، دخول
784.89499، وقف 781.99642، هدف 792.63085): حساب يدوي بسيط للأرقام المرسَلة
فعلاً أعطى RR≈2.67، بينما قاعدة البيانات وكل مكان يعرض الرقم للمستخدم
أظهر **1.0** لنفس الإشارة بالضبط — وتأكّد إنه بلاغ قديم لا علاقة له بأي
تعديل اليوم (إشارة من 2026-09-17 عندها نفس العطل).

**تتبّع أثر القرار الكامل بلوقات الإنتاج** (`docker logs` لحظة الإصدار)
كشف السبب بدقة، بدل التخمين:
- `_institutional_gate` (Rule 7): يحسب RR الأولي من `tp1` الأصلي ويخزّنه
  بـ`levels["risk_reward"]` و`analysis["risk_reward_ratio"]`.
- **Rule 10** (نفس الدالة، أسطر تالية): يوسّع `levels["tp1"]` بناءً على أقرب
  مستوى سيولة (BSL/SSL) — هدف أبعد وأفضل غالباً — **لكن لا يحدّث RR
  المخزَّن إطلاقاً**، فيبقى يعكس الهدف الضيّق القديم.
- الدالة نفسها تحسب RR **صحيحاً من جديد** بعد Rule 10 وتخزّنه بحقل ثالث
  منفصل `gate_rr` — لكن **حقل واحد بس** (`_confidence_calibration_layer`)
  يقرأ `gate_rr` أولاً. كل الباقي (`_classify_trade_mode`، `bot.py`، بوت
  تلغرام `save-alert-signal`، أي استهلاك لـ`risk_reward_ratio` من القاعدة)
  يقرأ الحقل القديم الفاسد مباشرة.
- دليل قاطع من اللوق نفسه بنفس اللحظة: `GATE PASSED ... rr=2.67` (يطبع
  `rr_final` الصحيح) ثم بعدها بسطرين `SMART_MODE ... rr=1.0` (يقرأ الحقل
  القديم) — **نفس التحليل، رقمان مختلفان**، قاطع إنه حقلان لا مصدر حقيقة
  واحد.

**الأثر المؤكَّد قبل التنفيذ (فحص، لا افتراض):**
- ✅ **قرار الشراء/البيع نفسه سليم دائماً** — البوابة تستخدم RR الصحيح
  (بعد Rule 10) للمقارنة مع الحد الأدنى، وترفض/تقبل بناءً عليه بشكل صحيح.
- ✅ **الدخول/الوقف/الهدف المُرسَلة للمستخدم صحيحة** — لم تتأثر بالعطل.
- ✅ **تصنيف الربح/الخسارة (TP_HIT/SL_HIT) سليم** — من حركة السعر الحقيقية.
- ❌ **الرقم المعروض/المخزَّن فقط كان أقل من الحقيقي دائماً** — يظلم جودة
  الإشارات أمام المستخدم ويلوّث أي متوسط RR بالتقارير (`avg_rr` بأداة
  التشخيص، عمود RR بلوحة الإدارة).

**التعديل المطبَّق (نطاق ضيّق، نفس مكان الحساب الصحيح بالضبط):**
بعد `analysis["gate_rr"] = rr_final` مباشرة، أُضيف سطران يزامنان
`levels["risk_reward"]` و`analysis["risk_reward_ratio"]` مع نفس
`rr_final` الصحيح — بدل ثلاث نسخ متفرّقة تتحمّل الانحراف، مصدر واحد
يُنسخ للحقلين الآخرين فور حسابه. **لم يُمسّ:** منطق Rule 10 نفسه، ولا أي
حدّ RR أدنى، ولا أي قرار قبول/رفض — تصحيح قراءة/عرض فقط.

**أثر جانبي محتمَل مقصود لا خطر منه:** `_classify_trade_mode`'s SCALP check
(`rr >= 1.0`) سيقرأ الآن الرقم الحقيقي الأعلى غالباً (Rule 10 يوسّع الهدف
دائماً لا يضيّقه) — يعني تصنيف SCALP/SWING قد يصير أدق لا أقل، ولا يوجد
مسار يصير RR الحقيقي أقل من القديم المخزَّن (Rule 10 يزيد المسافة للهدف
فقط)، فلا خطر رفض إشارة كانت ستُقبل سابقاً.

---

### PERFORMANCE_TIGHTENING كان يتغذّى على رفض لا علاقة له بالعتبة — استثناء جزئي من تجميد 17/09
**الحالة:** `مطبَّق` — استثناء محدود من التجميد بقرار صريح من صاحب المنتج، لا رفع كامل له

**بلاغ حقيقي بدأ الموضوع:** صاحب المنتج — "الوضع سيء جدا، مافي اشارات توصل
إطلاقاً". التتبّع (بالتفصيل الكامل بالمحادثة، لا هون) وصل لسلسلة اكتشافات:

1. الماسح التلقائي (`market_scanner.py`) شغّال طبيعي (60 محاولة/دورة)، لكن
   `actionable=0` لأكتر من 20 دورة متتالية (~11 ساعة).
2. آخر صف بجدول `signals` كان `created_at` = 2026-09-21 11:19:47 UTC —
   **صفر إشارة جديدة لأكتر من 21 ساعة**، مؤكَّد من قاعدة البيانات مباشرة
   لا من تجميع السكانر فقط.
3. أداة التشخيص الحيّة (`GET /api/v1/admin/diagnostic`) أظهرت الرفض شرعي
   ظاهرياً: 60% `zone_veto`، 30% `risk_integrity`، 10%
   `quality_below_threshold` — كله "فيتو صارم صحي" حسب تصنيف الأداة نفسها.
4. **لكن** لوقات الإنتاج (`docker logs moshapi_backend`) كشفت الآلية
   الفعلية: `🔒 PERFORMANCE_TIGHTENING [SYMBOL/TF] pass_rate=0% →
   delta+3 RR+0.1` تتكرر عبر أغلب الرموز بلا استثناء. والسبب الجذري:
   `_record_analysis_result` (كان) يسجّل `passed=False` لأي رفض **بلا
   تمييز نوعه** — بما فيها `htf_veto`/`zone_veto`، وهما **فيتو غير مشروط**
   («HTF_DIRECTION_CONFLICT → REJECTED, no delta exception» — حرفياً
   بـ`_decision_finalizer`) لا علاقة لهما بعتبة الدرجة أو RR إطلاقاً.
   فحين تزامن رفض أغلب الرموز بفيتو اتجاه/منطقة (سوق هادئ أو متزامن
   الحركة)، صار `pass_rate` يهبط لـ0% وتُشدَّد كل الرموز رداً على رفض
   **التشديد نفسه عاجز عن منعه** — حلقة تُغذّي نفسها بلا علاقة بجودة
   الإشارات الفعلية.

**التأكيد قبل التنفيذ (لا حكم بالانطباع):** تقرير الأداء (`/admin/performance-report?days=7`)
أظهر 53 قراراً فريداً مغلقاً منذ خط الأساس 2026-09-16 (فوق حد الـ40
المطلوب لحكم تجربة الـ1R)، نسبة نجاح 32% — بمنطقة "تمديد المراقبة" الوسطى
(بين 28% و33%) المتفق عليها مسبقاً بـ17/09. يعني: **البيانات كانت كافية
والحكم على تجربة الـ1R قائم فعلاً** — المشكلة المكتشفة هون منفصلة تماماً
عن نتيجة تلك التجربة، وهي عطل بآلية معايرة أعمّ (PERFORMANCE_TIGHTENING)
كان موجوداً على الأغلب قبل 17/09 أيضاً، وتجربة الـ1R لم تكن سببه.

**التعديل المطبَّق (نطاق ضيّق، لا مساس بأي منطق قرار آخر):**
- `_record_analysis_result(symbol, timeframe, passed, delta, hard_veto=False)`
  — حقل جديد `hard_veto` يُسجَّل مع كل محاولة.
- نقطة الاستدعاء بـ`analyze_market`: `hard_veto = decision_layer in
  ("htf_veto", "zone_veto")` — نفس التصنيف يلي تستخدمه أداة التشخيص
  الحيّة أصلاً (`_classify_decision_layer`)، لا منطق جديد مخترَع.
- `_get_pass_rate`: يستثني محاولات `hard_veto` من العيّنة بالكامل (لا
  رابحة ولا خاسرة) قبل حساب النسبة، وقبل فحص "عيّنة < 3 ⇐ افتراض سليم".
- **لم يُمسّ:** `risk_integrity` (يشمل رفض RR فعلي — له علاقة حقيقية
  بالعتبة فبقي محسوباً)، ولا `_get_avg_delta`، ولا أي قرار اتجاه/مستوى.

**لماذا استثناء لا رفع كامل للتجميد:** التجميد الأصلي (17/09) يحمي تجربة
الـ1R من تشويش متغيرات إضافية أثناء قياسها — وهذا التعديل **لا يمسّ منطق
الـ1R ولا أي قرار اتجاه/مستوى**، فقط يمنع آلية معايرة منفصلة من معاقبة
رفض لا تقدر تمنعه. باقي الملف (منطق الإشارة، الأهداف، الاتجاه) ما زال
مجمَّداً كما هو حتى نهاية فترة المراقبة.

**ما لم يُحسم:** مصير تجربة الـ1R نفسها (32%، منطقة تمديد) لسه بانتظار
موعدها الأصلي 2026-09-24 — هالقرار منفصل ولم يُتّخذ هون.

---

## 2026-09-21

### ودجت الشات: اسمه «تحليل ذكي» / Smart Analysis (كان «كفيل»)
**الحالة:** `مطبَّق` — الواجهة + ردود الخلفية الثابتة. التعريب الكامل لردود الوكيل `معلّق`.

**قرار صاحب المنتج:** الاسم «تحليل ذكي»، والودجت بلغتين ويعبّر شكله عن الذكاء.
«كفيل» بقي فقط كاسم عربي **للمنصة** (مقال المدونة) — لا علاقة له بالودجت.

**ما تغيّر:**
- الاسم مركزي بـ`frontend/src/components/SmartAnalysisBrand.jsx` (`BRAND`)؛
  الخلفية لها نسختها (`public_chat.py`، برومبتات `chat_agent.py`) — تُغيَّر معاً.
- `ChatBot.jsx` و`PublicChatBot.jsx` يتبعان `useLang`، ويرسلان `lang`
  لـ`/chat/message` و`/public/chat`.
- `public_chat.py`: ردود ثابتة بالعربي والإنجليزي. `chat_agent.py`: تحية
  إنجليزية، وسطر واحد بنظام Groq **للإنجليزية فقط** (بلا كلفة توكن على العربية —
  التزاماً بقرار إزالة +376 توكن الموثَّق بأرشيف «كفيل»).
- التحية كانت تقول «مُوش» بينما البرومبت يقول «كفيل» — تناقض قديم، وُحّد.

**⚠️ ما لم يُعرَّب (معلّق):** قالب رد التحليل `_build_direct_response` وإجابات
`platform_kb` وتوليد الشرح المحلي — عربية دائماً. مستخدم الإنجليزية يرى بطاقة
الأرقام المهيكلة بالإنجليزية ونص التحليل حولها بالعربية. مشروع مستقل.

**سطر «ليس نصيحة مالية» بالواجهة إخلاء مسؤولية فقط** — لا يعوّض الضوابط
المُزالة (منع اختراع أسعار/وعد بنسبة نجاح)، وتلك تبقى موثَّقة كفاقد بالأرشيف.

### قسم «الأسواق المدعومة» بالرئيسية: أيقونات SVG وفلتر فئات
**الحالة:** `مطبَّق` — عرض فقط (`MarketsShowcase.jsx`، `Landing.jsx`).

الإيموجي (🥇🛢📈) استُبدلت بأيقونات مرسومة لكل أصل. القائمة (8 رموز، «الأكثر
تداولاً» للذهب، «قريباً» لناسداك) **كما كانت حرفياً** — لم يُغيَّر ما يُدَّعى
دعمه، فقط شكله. الفلتر يخفّت غير المطابق ولا يزيله (تفادياً لقفز التخطيط
ولأن عناصر `.reveal` تُرصد عند التحميل فقط).

### باني الاستراتيجيات: تعريب كامل للصفحة يتبع مبدّل لغة الموقع
**الحالة:** `مطبَّق` — نُفّذ بالجلسة المنفصلة المخطَّط لها (راجع الخطة تحت)

**نقطة البداية:** شرط "Session Filter" باستراتيجية مستخدم كان مضبوطاً
"الكل" (AND) على 3 جلسات (لندن/نيويورك/آسيا) — **مستحيل رياضياً** (آسيا
00–09 لا تتقاطع إطلاقاً مع نيويورك 12–21)، فالاستراتيجية ما كانت
لتُطلق أبداً. أُصلح بإضافة مصطلحات عربية لأزرار منطق المجموعة
(AND/OR/X-of-Y ⇒ الكل/واحد على الأقل/عدد محدد) — `مطبَّق` فعلاً،
commit `f20addc`.

**الطلب الأوسع اللي تلاه:** تعريب الصفحة **كاملة** بحيث تتبع مبدّل لغة
الموقع (عربي ⇒ عربي، إنجليزي ⇒ إنجليزي) — لا الأزرار بس.

**حجم الشغل الفعلي (فُحص الكود، لا تخمين):**
- `StrategyBuilder.jsx` (5 تبويبات: البناء/المنطق/تلجرام/المراقبة/المحفوظة)
  — **~100+ نص عربي بلا نسخة إنجليزية إطلاقاً**، والصفحة **غير مربوطة
  بـ`LangContext`/`isAr` أصلاً** (فحص مباشر: صفر استخدام بالملف)
- أسماء الفئات الأربع (`CAT` — مؤشرات فنية/ICT-SMC/التذبذب/الجلسات)
  **جاهزة بلغتين أصلاً** بالكود (`label` عربي + `en` إنجليزي) — بس غير
  مربوطة بالمبدّل. شغل سريع.
- أسماء الشروط الـ18 الفردية (RSI, MACD, BOS, CHoCH, Order Block, FVG,
  Liquidity Sweep, Equal Highs/Lows, Premium/Discount, Kill Zone,
  London/New York/Asian Session, Kill Zones, ATR, اتجاه الصفقة) — كل
  وحدة عندها **وصف عربي جاهز** بالفعل، وتحتاج اسماً إنجليزياً واضحاً
  (أغلبها اختصارات عالمية تبقى كما هي بالعربي والإنجليزي — RSI/MACD/
  BOS/FVG لا تُترجَم فعلياً حتى بالمحتوى العربي المتخصص)
- `StrategyGuide.jsx` (لوحة الإرشاد) — نصوص شرح طويلة، عربي بالكامل
- `strategy_engine.py` (الخادم) — رسائل سجل المراقبة ورسائل تلجرام
  المولَّدة تحمل عربي ثابت بالكود (مثال: `"الكل"`/`"واحد على الأقل"`
  بسطر ~391) — تحتاج نسخة إنجليزية مشروطة بلغة المستخدم المستلم

**لماذا جلسة منفصلة لا الآن (حين خُطِّط):** طول هالجلسة الحالية (انضغطت مرة
سلفاً) + حجم الشغل الحقيقي عابر لملفات خلفية وواجهة معاً — أنسب ببداية سياق
نظيفة. **الملف `strategy_engine.py` مسموح لمسّه** (منفصل عن `ai_engine_v5`/
`ict_engine` المجمَّدين — راجع التمييز الموثَّق مسبقاً بين توصيات
المنصة وباني الاستراتيجيات)، فلا تعارض مع التجميد الحالي.

**ما نُفِّذ (2026-09-21، الجلسة المنفصلة):**
- `StrategyBuilder.jsx` + `StrategyGuide.jsx`: ربط بـ`useLang`/`isAr` وتعريب
  كل نص عربي كان بلا نسخة إنجليزية (أزرار، toasts، تلميحات، حالات فارغة،
  نصوص لوحة الإرشاد التشخيصية المولَّدة ديناميكياً بـ`diagnoseBuild`).
- **قرار تصميم متعمَّد: لم تُمسّ القيم الكامنة (canonical values)** —
  `TF_MODES` (تبقى عربية بالتخزين، تُعرض إنجليزية عبر `TF_MODE_LABEL_EN`
  فقط)، ومصطلحات ICT/SMC التقنية (RSI, MACD, BOS, FVG, Entry, SL, TP, RR,
  Weight, Score...) تبقى إنجليزية بأي لغة — نفس اصطلاح المكتبة المتخصصة
  الموثَّق أصلاً بالكود، وتفادياً لكسر التوافق مع استراتيجيات محفوظة سلفاً
  (`backend/app/api/strategies.py` يخزّن `tfMode` كنص حرّ يُطابَق حرفياً).
- `strategy_engine.py::evaluate_strategy`: `block_reason` (يظهر بلوحة
  "المراقبة الحية") صار يُبنى بالعربي **والإنجليزي معاً** — حقل جديد
  `block_reason_en` بجانب `block_reason` الأصلي، يمرّان سوية عبر
  `strategy_checker.py` إلى `diagnostics_json` (عمود JSON مرن، لا حاجة
  لمايجريشن). الواجهة تختار حسب `isAr`. **لم يُمسّ** `build_telegram_message`
  ولا أي نص يصل تلغرام — بوت تلغرام عربي بتصميمه (راجع CLAUDE.md)، فلا علاقة
  لمبدّل لغة الموقع به.
- **خارج النطاق عمداً:** لا مطابقة كاملة لاتجاه RTL/LTR على مستوى CSS
  (`text-right`, `mr-auto`, إلخ) — بقيت كما هي، فقط `dir` الجذر يتبدّل. نفس
  المستوى المتّبع بصفحات لوحة التحكم الأخرى (`Dashboard.jsx` وغيرها)، وليس
  طلباً موثَّقاً من صاحب المنتج.

---

### عمود "مستخدمون" بتقارير الأداء كان يعدّ صفوف Signal لا المستلمين الحقيقيين
**الحالة:** `مطبَّق` — تعديل عرض معزول، لا يمسّ المحرك ولا حلقة الرصد

**بلاغ حقيقي:** تقرير يومي أظهر NAS100 بـ5 "مستخدمين" وALMARAI بـ2 وSP500
بـ3، رغم توحيد سابق يفترض أن كل مشترك بلا watchlist يستلم كل شي (130 من
144 مشترك مفعّل تلجرام). السؤال: ليش الرقم أصغر بكثير من 130؟

**السبب (تأكد بالكود، لا تخمين):** جدول `Signal` يُنشأ صف منفصل لكل
مستخدم **حلّل الرمز بنفسه شخصياً** عبر مراقبته الخاصة (`monitor_watchlists`
بـtelegram-bot/bot.py) — يعني `user_count`/`total_rows` كانا يعدّان
**"كم شخص عنده الرمز بقائمته الشخصية"**، لا **"كم شخص استلم رسالة
تلغرام فعلياً"**. البث العام (`broadcast_new_signals`) يصل فعلياً لعشرات
المشتركين (كل بلا watchlist + من قائمتهم تضم الرمز) **بلا أي صف Signal
يمثّلهم** — نفس العطل بالضبط المُصلَح بلوحة "كل الإشارات" بـ16/09
(`SignalDelivery`, `delivered_count`)، لكن التقرير اليومي والمعاينة
(`/admin/performance-report`) لم يُحدَّثا وقتها — إصلاح بمكان نُسي بمكان آخر.

**الإصلاح:** بـ`app/api/admin.py` فقط (لا `decision_grouping.py` —
مشترك مع معايرة `ai_engine_v5.py`، لم يُلمس عمداً):
- `_build_performance_report`: `total_rows` صار مجموع `SignalDelivery`
  الحقيقي (`ok=True`) بدل `len(signals)`، ونص الرسالة تغيّر من "وُزّعت
  على X صفقة مستخدمين" إلى "وصلت فعلياً لـX مستخدم" (يعكس المعنى الجديد)
- `get_performance_report` (نقطة المعاينة): `d["user_count"]` لكل قرار
  يُستبدَل بعدد التسليم الحقيقي (`SignalDelivery` بمعرّف الصف الممثِّل
  `d["id"]`)، بعد التجميع لا قبله — بدون تعديل `group_unique_decisions()`
  نفسها

**لماذا آمن رغم التجميد:** `win_rate`/`wins`/`losses`/`النقاط` كلها
محسوبة من `status`/`points` داخل `decisions` — **لا علاقة لها بـ
`user_count`/`total_rows` إطلاقاً**. التعديل معزول 100% بطبقة العرض،
بعد أن ينتهي التجميع والحساب. لم يُلمس أي ملف يستورده `ai_engine_v5.py`.

**تصحيح فوري بنفس اليوم — المحاولة الأولى كانت تفشل دائماً بصمت:**
بُحث بـ`d["id"]` وحده (معرّف "الممثِّل" حسب `decision_grouping`)، وهو
**أقدم** صف بالمجموعة (`rows[0]` بعد ترتيب تصاعدي) — بينما
`/bot/new-signals` يبثّ فعلياً عن **أحدث** صف بنفس المجموعة (ترتيب
`desc`، أول تكرار محتفَظ به بالحلقة). معرّفان مختلفان تماماً، فالبحث
بواحد يخطئ الصف اللي عليه سجلّ `SignalDelivery` الحقيقي **دائماً** —
يرجع صفراً فيسقط لقيمة `len(rows)` القديمة نفسها، والمستخدم شافها "نفس
الشي" رغم نشر الإصلاح فعلياً.

**الإصلاح الصحيح:** أُضيف حقل إضافي بحت `row_ids` لـ`_finalize_decision_group`
(كل معرّفات صفوف المجموعة، لا واحد مفترَض) — إضافة مفتاح جديد فقط، بلا
تعديل أي مفتاح أو قيمة موجودة، فمعايرة `ai_engine_v5.py` غير متأثرة
إطلاقاً. البحث بـ`admin.py` صار يغطي كل `row_ids` لكل قرار ويجمعها،
بدل معرّف واحد مخمَّن.

**درس عام:** أي "صف ممثِّل" (representative row) مفترَض بين مسارين
مستقلين (هون: اختيار البث بترتيب `desc`، واختيار التجميع بترتيب
`asc`) يجب التحقق من تطابقهما صراحة، لا افتراض أنهما نفس الصف.

---

## 2026-09-19

### "الدفع بالبطاقة" صار عبر PayPal Advanced Card Payments — بلا أي اسم معالج ظاهر للعميل
**الحالة:** `مطبَّق` — بانتظار تفعيل المفاتيح من صاحب المنتج (لوحة الإدارة)

**الطلب الصريح من صاحب المنتج:** زر "الدفع بالبطاقة" يبقى بلا أي اسم معالج
مذكور (لا Stripe ولا PayPal)، يفتح نموذج بطاقة عادي (رقم/تاريخ/CVV)، ويُعالَج
بالخلفية عبر PayPal. **وهذا مو حيلة** — هو منتج رسمي من PayPal اسمه
"Advanced Card Payments": حقول بطاقة مستضافة بلا شعار، مصمَّم لنفس الغرض.
يحتاج تفعيل/أهلية خاصة على حساب PayPal Business (تُمنح حسب الدولة والنشاط) —
لم تُتحقَّق وقت التنفيذ؛ صاحب المنتج طلب "اشتغل ع اساس انها فعالة" فنُفِّذ
على هذا الافتراض. إن ثبت عدم الأهلية لاحقاً، `cardFields.isEligible()`
بالفرونت يُرجع `false` ويظهر تنبيه بديل بدل نموذج معطَّل صامت — **لا عطل
صامت، بل رسالة واضحة**.

**لماذا PayPal لا Stripe (رغم إن Stripe مدمج مسبقاً بنفس الأسلوب):**
لم يُذكر السبب صراحة، لكن الطلب جاء بعد نقاش مطوَّل حول أهلية الحساب —
الأرجح قيد إقليمي/تشغيلي على Stripe لم يُفصَح عنه. **Stripe لم يُحذف** —
بقي بالكود والـ`/stripe/*` endpoints فعّالة، فقط غير مستدعاة من الواجهة.
سبب الإبقاء: قرار المستخدم الصريح "ما بدي نحذف شيء يأثر علينا مستقبلاً".

**البنية (تطابق نمط Stripe/Spaceremit الموجود عمداً — نفس نقطة التسعير
الموحّدة `_priced()`، نفس مبدأ التحقق من طرف الخادم لا الثقة بالعميل):**
- `POST /subscription/paypal/create-order` — يُنشئ PayPal Order، يُثبّت
  `custom_id = "u{user_id}|{plan}|{coupon}"` وقت الإنشاء من طرف الخادم.
- `POST /subscription/paypal/capture-order` — يُستدعى من الفرونت فور
  `CardFields.submit()`. **التفعيل متزامن هنا فعلياً** (بخلاف Stripe
  webhook-dependent) — نفس الطلب يُحصّل الدفعة من PayPal ويفعّل الاشتراك.
  **يتحقق أول شيء إن `custom_id` المرفَق بالطلب يطابق المستخدم المصادَق
  عليه حالياً** قبل أي تفعيل — وإلا لأمكن لمستخدم تمرير `order_id` شخص
  آخر وتفعيل اشتراكه هو بدفعة غيره. هذا فحص لا يوجد نظيره عند Stripe لأن
  Stripe يعتمد `metadata` موقَّعة بتوقيع Webhook لا يمكن التلاعب بها من
  العميل أصلاً؛ PayPal Orders API لا يوقّع البيانات المُعادة لعميل، فوجب
  التحقق يدوياً.
- `POST /subscription/paypal/webhook` — شبكة أمان فقط، **اختيارية**
  (تعمل فقط لو `paypal_webhook_id` مُعدّ بالداشبورد)، تتحقق من توقيع
  PayPal عبر `verify-webhook-signature` قبل أي ثقة بجسم الطلب — نفس مبدأ
  Spaceremit ("لا نثق بجسم الويبهوك القادم من الشبكة مباشرة").
- `idempotent` على `tx_id = capture_id` — إعادة محاولة الفرونت أو وصول
  الويبهوك بعد `/capture-order` لا يُفعِّل الاشتراك مرتين.
- **لا تعديل على جدول `Payment`** — `provider`/`network`/`tx_id` عامة
  أصلاً، تكفي بلا migration.
- `/subscription/plans` → `card_payment_enabled` كان يُشتَق من جاهزية
  Stripe، صار من جاهزية PayPal — **الاسم بقي كما هو** لأنه عام أصلاً ولا
  يُسرّب اسم المعالج.

**المفاتيح تُدار من لوحة الإدارة لا من `.env.prod`** — طلب صريح من صاحب
المنتج ("بدي اقدر اضيفهم من الداشبورد نفسه"). أُضيف قسم PayPal بـ
`Admin.jsx` بنفس نمط قسم Spaceremit تماماً (مفتاح live + مفتاح اختبار +
مفتاح Webhook اختياري + مفتاح تفعيل + مفتاح وضع الاختبار)، يقرأها الخادم
عبر `_setting()` (قاعدة البيانات أولاً، fallback بيئة فارغ افتراضياً).
`config.py` يحمل حقولاً فارغة كاحتياط فقط — المسار المقصود هو الداشبورد.

**الواجهة:** مكوّن جديد `PayPalInlineCheckout.jsx` بنفس تصميم/ألوان نموذج
البطاقة السابق بالضبط (العميل ما يلاحظ فرق). سطر الثقة أسفل النموذج صار
"دفع آمن ومشفّر بالكامل" بلا ذكر أي معالج (كان "مدعوم من Stripe" — سُحب).

**اعتماد جديد:** `@paypal/paypal-js` (المُحمِّل الرسمي لسكربت PayPal JS
SDK، لا يوجد غلاف React رسمي لمكوّن `card-fields`).

---

### تأكيد: الحساب غير مؤهّل لـAdvanced Card Payments — بديل مؤقت يعمل بلا شرط أهلية، ويترقّى تلقائياً
**الحالة:** `مطبَّق` — إجراء مؤقت بقرار صاحب المنتج

**الاختبار الحي حسم الشك:** `cardFields.isEligible()` رجع `false` رغم
صحة المفاتيح ونجاح إنشاء الطلب (`create-order` اشتغل، والاتصال بـPayPal
سليم) — الحساب فعلاً غير مفعّل له حقول البطاقة بلا شعار. صاحب المنتج
اختار: **"فعّل زر PayPal العادي الآن مؤقتاً"** بدل الانتظار لموافقة
PayPal أو التعطيل الكامل.

**التنفيذ — بلا أي تعديل بالخلفية:** نفس نقطتي `create-order`/
`capture-order` بالضبط (الطلب لا يهتم كيف تم تأكيده بالفرونت). المكوّن
`PayPalInlineCheckout.jsx` صار يحمّل `components: 'buttons,card-fields'`
معاً، يجرّب حقول البطاقة أولاً، وعند `isEligible() === false` يعرض زر
PayPal القياسي (`paypal.Buttons`) بدل رسالة خطأ — نفس `createOrder`/
`onApprove` بالضبط.

**لماذا هذا "مؤقت" لا حل دائم يحتاج قراراً لاحقاً:** الزر القياسي
**يبيّن شعار PayPal** — يخالف الطلب الأصلي (بلا اسم معالج ظاهر). لكنه
**يترقّى تلقائياً بلا أي نشر إضافي**: فور ما PayPal يوافق على تفعيل
Advanced Card Payments على الحساب، نفس الكود يتحقق من `isEligible()`
بكل تحميل صفحة ويتحول تلقائياً لحقول البطاقة بلا شعار — لا حاجة نرجع
نلمس الكود يوم تُوافَق الأهلية.

**الخطوة المطلوبة من صاحب المنتج (خارج الكود):** التواصل مع دعم PayPal
لطلب تفعيل "Advanced Credit and Debit Card Payments" على الحساب —
راجع developer.paypal.com/dashboard/applications/live.

**تحديث 20/09 — رد دعم PayPal المباشر (شات) يؤكد التشخيص ويحسم المتطلب:**
الحساب **غير مؤهّل حالياً** فعلاً (مو خلل كود — تأكيد رسمي من PayPal)،
والشرط الدقيق: **إضافة وتأكيد بطاقة خصم/ائتمان حقيقية على حساب PayPal
Business نفسه** (paypal.com → Wallet، لا developer.paypal.com) — اقترح
الدعم بطاقة من بنك أردني محلي تحديداً. التأكيد عادة برسوم صغيرة مؤقتة
تظهر بكشف الحساب. **لا كود يُغيَّر لهذا** — `cardFields.isEligible()`
بالكود الحالي يعيد الفحص تلقائياً كل تحميل صفحة، فالتفعيل يحصل من نفسه
فور موافقة PayPal داخلياً (قد تستغرق ساعات–يوماً بعد تأكيد البطاقة).

**⚠️ بلاغ حقيقي 19/09 — عطل كان يسحب المال بلا تفعيل اشتراك:**
`capture-order` أرجع "هذا الطلب لا يخصّك" لمستخدم يدفع بحسابه هو. السبب:
PayPal تُعيد `custom_id` على كائن **الـcapture نفسه**
(`purchase_units[0].payments.captures[0].custom_id`) بردّ التحصيل، لا
بالضرورة على غلاف `purchase_unit` (`purchase_units[0].custom_id`) الذي
كان الكود يقرأه وحده. فالتحقق يفشل **دائماً** رغم صحة الملكية — و**المال
يُسحب فعلياً عند PayPal قبل الرفض**، لأن الرفض يقع بعد نجاح `/capture`
لا قبله. **الأصل الموثوق هو كائن الـcapture، لا غلاف purchase_unit.**
تصحيح للفهم السابق: صيغة `custom_id` بحد ذاتها سليمة (`u{id}|{plan}|{coupon}`)،
والعطل بمكان القراءة فقط لا بالتوليد.

**أُصلح فوراً** (`capture.get("custom_id") or pu.get("custom_id")`)،
وأُضيفت أداة `paypal_reconcile_order.py` لاسترجاع أي طلب سُحب ماله فعلاً
قبل الإصلاح ولم يُفعَّل — تستعلم PayPal مباشرة بـorder_id وتُفعّل
idempotent فقط لو COMPLETED فعلاً وغير مسجَّل مسبقاً.

**وأُصلح عطل ثانٍ مرتبط:** إعادة محاولة الدفع بعد فشل `capture-order`
كانت تُبقي نفس `order_id` المُستهلَك (PayPal تُحصِّل الطلب مرة واحدة
فقط) — فيفشل أي إعادة محاولة **دائماً** بصمت. صار يُطلب طلب جديد فوراً
عند أي فشل تأكيد.

**تحديث 19/09 (نفس اليوم):** زر PayPal الافتراضي بلا `fundingSource`
يعرض عدة أزرار مكدّسة (PayPal + Venmo + Pay Later حسب الأهلية) — طلب
صاحب المنتج **زراً واحداً بس "Debit or Credit Card"**. صار
`fundingSource: paypal.FUNDING.CARD` مع رجوع للافتراضي لو هذا المصدر
تحديداً غير مؤهَّل (نادر). وأُضيف `shipping_preference: NO_SHIPPING`
لطلب الإنشاء بالخادم — اشتراك رقمي بلا شحن، فلا داعي يُطلَب عنوان شحن
من العميل (بيانات البطاقة/الفوترة تبقى مطلوبة، PayPal تفرضها لأي بطاقة).

**تحديث آخر 19/09: أُضيف Apple Pay كخيار إضافي فوق البطاقة** —
`paypal.Buttons({fundingSource: paypal.FUNDING.APPLEPAY})` بنفس
`create-order`/`onApprove`. **يفحص أهليته بنفسه** (جهاز/متصفح Apple +
بطاقة محفوظة بـWallet + تفعيل الحساب) ولا يعرض شيئاً لو غير مؤهَّل —
بلا شرط تفعيل مسبق منّا، وبلا أي أثر على مسار البطاقة إن غاب أو فشل.
لم يُتحقَّق من تفعيل الحساب لخاصية Apple Pay على لوحة PayPal مسبقاً —
نفس أسلوب "جرّب حياً بدل التخمين" المتّبع بكل قرارات PayPal اليوم.

**تحديث 20/09 — قياس حاسم من جهاز حقيقي (لوحة تشخيص `?ppdebug=1`،
راجع `paypal_reconcile_order.py` بنفس الروح):** آيفون Safari، بطاقة
فعّالة مؤكَّدة بـWallet تعمل بمواقع أخرى:

```
window.ApplePaySession: true   canMakePayments(): true   ← الجهاز سليم 100%
applePay isEligible(): false   cardFields isEligible(): false
error: null                    ← لا استثناء، PayPal رجعت "لا" بوضوح
```

**يحسم نهائياً:** لا علاقة للجهاز ولا المتصفح ولا الكود — **PayPal لم
تُفعّل Apple Pay ولا Advanced Card Payments على الحساب التجاري بعد**،
نفس السبب لكلتا الميزتين. لا إجراء كود متبقٍ؛ ينتظر فقط موافقة PayPal
الداخلية (أو سؤال دعمهم صراحة عن Apple Pay كخاصية منفصلة عن الكروت).

**تحديث 22/09 — دعم PayPal قال "ما فيه شي من طرفنا"، فأُعيد الفحص بجيل
SDK ثانٍ كلياً لنفي فرضية عطل الإصدار:** صاحب المنتج راجع الدعم وجاءه رد
عام (رابط توثيق لا علاقة له بالأهلية تحديداً، ثم "حسابك فعّال ومافي شي
بده تفعيل"). بدل التسليم بتفسير غامض، أُعيد فحص الأهلية **بجيل SDK مختلف
كلياً**: PayPal Web SDK v6 (`createInstance`/`findEligibleMethods` →
`isEligible('advanced_cards')`) بدل الكلاسيكي (`sdk.js` →
`cardFields.isEligible()`).

- محاولة أولى (عزل بـiframe): فشلت — `No ack for postMessage: pixelReady`.
  بكسل كشف الاحتيال الداخلي بسكربت v6 يرفض العمل بأي iframe متداخل
  (بغض النظر عن العزل)، يحتاج نافذة top-level حقيقية.
- محاولة ثانية (بنفس صفحة الدفع الحية، بعد الكلاسيكي): فشلت —
  `createInstance is not a function`. السببان يكتبان على `window.paypal`
  بنفس الصفحة؛ v6 يتصرّف دفاعياً ولا يستبدل `window.paypal` الموجود أصلاً
  من الكلاسيكي، فما يضيف `createInstance` إطلاقاً — مشاركة namespace
  بنيوية، لا مسألة ترتيب تحميل، ولا حل ممكن بنفس الصفحة.
- **الحل: صفحة معزولة كلياً `/paypal-v6-diag`** (خلف تسجيل الدخول، بلا أي
  سكربت PayPal تاني بالصفحة) — النتيجة نظيفة: `isEligible('advanced_cards')
  = false`، `error: null`.

**يحسم إغلاق فرضية "عطل بإصدار SDK واحد":** جيلا SDK مستقلان تماماً على
خوادم PayPal يعطيان **نفس الجواب بالضبط** بلا أي خطأ تقني من الاثنين —
دليل أقوى بكتير للدعم من أي محادثة سابقة: الحساب مرفوض فعلياً على
مستوى الحساب/الأهلية، مو مشكلة تكامل أو إصدار مكتبة. الخطوة التالية
تخاطب الدعم بهالدليل المزدوج تحديداً، لا برد عام تاني.

**تحديث نفس اليوم (22/09) — تأكيد ثلاثي، الملف التقني مقفَل نهائياً:**
المستخدم شارك مثال PayPal الرسمي (فيديو تعليمي) يستخدم جيلاً **ثالثاً**
من الـAPI: `paypal.HostedFields` (الأقدم، سابق حتى لـCardFields). أُضيفت
صفحة معزولة ثالثة بنفس النمط `/paypal-hf-diag` (تجلب `client_id` من
`/paypal/create-order` الحقيقي نفسه، لا نسخة مكرّرة) — النتيجة: **نفس
الجواب بالضبط**: `HostedFields.isEligible() = false`، `error: null`.

**ثلاثة أجيال API مستقلة كلياً على خوادم PayPal (HostedFields الأقدم،
CardFields الكلاسيكي المستخدم بالدفع الحي فعلياً، Web SDK v6 الأحدث)
تتفق حرفياً على نفس النتيجة بلا استثناء واحد.** هذا يقفل أي احتمال متبقٍ
لعطل تكامل/إصدار مكتبة نهائياً — القرار حساب/أهلية بحت من طرف PayPal.
لا مزيد من الفحص التقني مجدٍ؛ الخطوة الوحيدة الباقية تواصل بشري مباشر مع
دعم PayPal يحمل الدليل الثلاثي أعلاه.

**أدوات التشخيص المعزولة الثلاث تبقى بالكود** (خلف تسجيل الدخول، صفر أثر
على الدفع الحي): `/paypal-v6-diag`، `/paypal-hf-diag`، و`?ppdebug=1` على
أي صفحة فيها `PayPalInlineCheckout` (الكلاسيكي + Apple Pay). تُستخدم
فوراً لإعادة الفحص بعد أي رد PayPal بلا حاجة لكتابة كود جديد.

**أدوات تشخيص متبقّية بالكود (خلف تسجيل الدخول/`?ppdebug=1`، صفر أثر على
الدفع الحي):** `/paypal-v6-diag` (v6 معزول)، `?ppdebug=1` على أي صفحة
فيها `PayPalInlineCheckout` (الكلاسيكي + Apple Pay). تُستخدم لأي فحص
أهلية مستقبلي بلا حاجة لكتابة كود جديد.

---

## الحالة الراهنة (2026-09-18)

**🧊 تجميد قائم على المحرك وحلقة الرصد** منذ 2026-09-17 — قرار صاحب المنتج.
لا تعديل على `ai_engine_v5` أو `ict_engine.calculate_levels` أو `bot.py`
(حلقة الرصد) حتى تنتهي مدة المراقبة. [تفصيل الأسباب بالأرشيف ↗](DECISIONS_ARCHIVE.md#تجميد-التعديلات--أسبوع-مراقبة-بلا-تغيير)

**⚠️ استثناءان مسجَّلان بـ2026-09-22** (كلاهما بإذن صريح من صاحب المنتج،
نطاق ضيّق، لا يمسّان منطق الـ1R ولا أي قرار اتجاه/مستوى):
1. إصلاح حلقة PERFORMANCE_TIGHTENING الذاتية التغذية.
2. تصحيح RR المعروض/المخزَّن (كان يعكس هدف Rule 10 القديم قبل التوسيع).

[التفصيل الكامل ↑](#2026-09-22)

**شرط الحكم على تعديل 1R** (مكتوب مسبقاً قبل النتيجة): بعد 7 أيام و≥40 قراراً
مغلقاً على بيانات ما بعد 2026-09-16 — نسبة ربح `≥33%` يُبقى، `<28%` يُتراجَع
فوراً، بينهما تُمدَّد المراقبة أسبوعاً.

**⚠️ قيد يُقرأ قبل أي حكم بالنتيجة:** `Rule 10` في `ai_engine_v5` (~س2496)
يكتب فوق أهداف تعديل 1R بمستويات سيولة متى وُجدت — فالتعديل يسري جزئياً لا
كلياً، وأي محاكاة يجب أن تنمذج `Rule 10` وإلا قاست وهماً.
[تفصيل ↗](DECISIONS_ARCHIVE.md#تعديل-الأهداف-يُكتب-فوقه-بـrule-10--أثره-جزئي-لا-شامل)

**قيدان دائمان يُقرآن قبل أي رقم أداء:**
- كل البيانات **قبل 2026-09-16** سُجّلت بحلقة رصد معطوبة — لا يُبنى عليها
  استنتاج استراتيجي.
- النقاط **ليست عملة موحّدة** بين الرموز (فارق المضاعف 1200 ضعفاً) — الحكم
  بمضاعف R دائماً.

---

## معلّق — بانتظار قرار أو انتهاء التجميد

| القرار | ينتظر | تفصيل |
|---|---|---|
| تتبّع نتائج تنبيهات باني الاستراتيجيات (لا تُرصد اليوم إطلاقاً) | نهاية أسبوع التجميد | [أرشيف](DECISIONS_ARCHIVE.md#تجميد-التعديلات--أسبوع-مراقبة-بلا-تغيير) |
| تنبيه تأمين الصفقة (نقل الوقف للدخول) وعتبته | نهاية أسبوع التجميد + قياس | [أرشيف](DECISIONS_ARCHIVE.md#تجميد-التعديلات--أسبوع-مراقبة-بلا-تغيير) |
| أرضية دنيا لمسافة الوقف | قياس الأثر (يُسقط تغطية بحجم غير معروف) | [أرشيف](DECISIONS_ARCHIVE.md#أرضية-دنيا-لمسافة-الوقف) |
| الفضة: هدف 1R لا يكفي وحده (35% تموت بربع الساعة الأول) | محاكاة تدخّل بمرجع صحيح | [أرشيف](DECISIONS_ARCHIVE.md#الفضة-المشكلة-في-الوقف-لا-في-الاتجاه--والفريم-15m-هو-البؤرة) |
| توحيد `PAID_PLANS` (الباقة السنوية مخزّنة كـ`monthly`) | تعديل مستقل، ليس عاجلاً | [أرشيف](DECISIONS_ARCHIVE.md#الباقة-السنوية-تُخزَّن-كـmonthly-على-المستخدم) |

---

## سجل مختصر — أحدث أولاً (التفصيل الكامل بالأرشيف)

### 2026-09-17
- **تعديل الأهداف يُكتب فوقه بـRule 10** — `مطبَّق` (وصف واقع، لا تغيير). [تفصيل](DECISIONS_ARCHIVE.md#تعديل-الأهداف-يُكتب-فوقه-بـrule-10--أثره-جزئي-لا-شامل)
- **تجميد التعديلات أسبوعاً** — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#تجميد-التعديلات--أسبوع-مراقبة-بلا-تغيير)
- **تأكيد تعديل الأهداف بمرجع صحيح للمعادن** (228 قراراً، XAGUSD 47%، XAUUSD 64%) — `مطبَّق ومؤكَّد`. [تفصيل](DECISIONS_ARCHIVE.md#تأكيد-تعديل-الأهداف--بمرجع-صحيح-للمعادن)
- **الفضة: المشكلة بالوقف لا الاتجاه، الفريم 15m هو البؤرة** — `معلّق`. [تفصيل](DECISIONS_ARCHIVE.md#الفضة-المشكلة-في-الوقف-لا-في-الاتجاه--والفريم-15m-هو-البؤرة)
- **كفيل: معرفة المنصة محلياً `مطبَّق`، النبرة والحدود `متراجَع عنه`** (+376 توكن بلا فائدة). [تفصيل](DECISIONS_ARCHIVE.md#كفيل-معرفة-بالمنصة-محلياً--والنبرة-والحدود-متراجَع-عنها)
- **كل رقم بالرسالة يُشتقّ من أرقامها الأخرى** (RR كان يُحسب على هدف مختلف عن المعروض) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#كل-رقم-بالرسالة-يُشتقّ-من-أرقامها-الأخرى)
- **الاستراتيجية لا تتحكّم بالاتجاه — أُضيف شرط له** — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#الاستراتيجية-لا-تتحكّم-بالاتجاه--وأُضيف-شرط-له)
- **شرط «الجلسة» كان يطابق نافذة kill zone لا الجلسة** (كانت تغطي 12 من 24 ساعة، صارت 22) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#شرط-الجلسة-كان-يطابق-نافذة-الـkill-zone-لا-الجلسة)
- **رسالة الاستراتيجية: الاتجاه من المستويات لا من توصية المحرك** — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#رسالة-الاستراتيجية-الاتجاه-من-المستويات-لا-من-توصية-المحرك)
- **لوحة إرشاد دائمة تفحص بناء الاستراتيجية قبل الحفظ** — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#لوحة-إرشاد-دائمة-تفحص-البناء-قبل-الحفظ)
- **باني الاستراتيجيات يعرض الدرجة ويخفي شرطها** (الإطلاق يشترط أمرين، كان يُعرض واحد) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#باني-الاستراتيجيات-يعرض-الدرجة-ويخفي-شرطها)
- **تشخيص الفضة الأول باطل** (قِيس بالعقود الآجلة) — `متراجَع عنه`. [تفصيل](DECISIONS_ARCHIVE.md#تشخيص-الفضة-الأول--باطل-قِيس-بالعقود-الآجلة)

### 2026-09-16
- **`await` على دالة متزامنة أوقف الرصد التلقائي** — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#await-على-دالة-متزامنة-أوقف-الرصد-التلقائي--ومصدر-السعر-فُصل-عن-مصدر-الشموع)
- **الهدف الأول 1.0R والثاني 2.0R بدل مسافة ثابتة بالـATR** (قرار صاحب المنتج بتفويض صريح) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#الهدف-الأول-عند-10r-والثاني-عند-20r-بدل-مسافة-ثابتة-بالـatr)
- **توسيع الوقف لا يفيد — الإصلاح تقريب الهدف الأول** — `مطبَّق` (استنتاج سابق `متراجَع عنه`). [تفصيل](DECISIONS_ARCHIVE.md#توسيع-الوقف-لا-يفيد--والإصلاح-هو-تقريب-الهدف-الأول)
- **أرضية دنيا لمسافة الوقف** — `معلّق`. [تفصيل](DECISIONS_ARCHIVE.md#أرضية-دنيا-لمسافة-الوقف)
- **كل البيانات قبل 2026-09-16 غير صالحة لاستنتاج استراتيجي** — `مطبَّق` (`OUTCOME_FIX_DATE`). [تفصيل](DECISIONS_ARCHIVE.md#كل-البيانات-قبل-2026-09-16-غير-صالحة-لاستنتاج-استراتيجي)
- **استنتاج «الشراء خاسر بنيوياً»** — `متراجَع عنه` (قيس بمصدر معطوب). [تفصيل](DECISIONS_ARCHIVE.md#استنتاج-الشراء-خاسر-بنيوياً--مُبطَل)
- **النقاط ليست عملة موحّدة — الحكم بمضاعف R** — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#النقاط-ليست-عملة-موحّدة--الحكم-بمضاعف-r)
- **قياس النتيجة بنفس مرجع بناء المستويات** (`_SPOT_SYMBOLS`) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#قياس-النتيجة-بنفس-مرجع-بناء-المستويات)
- **حكم المشي الزمني نهائي فقط متى غطّى عمر الإشارة** (`walked`) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#حكم-المشي-الزمني-نهائي-فقط-متى-غطّى-عمر-الإشارة)
- **حدّ صلب: التحقق التاريخي لا يتجاوز ~3.5 أيام** — `مطبَّق` (قيد بنيوي). [تفصيل](DECISIONS_ARCHIVE.md#حدّ-صلب-التحقق-التاريخي-لا-يتجاوز-35-أيام)
- **الباقة السنوية تُخزَّن كـ`monthly`** — `معلّق` (توحيد `PAID_PLANS`). [تفصيل](DECISIONS_ARCHIVE.md#الباقة-السنوية-تُخزَّن-كـmonthly-على-المستخدم)
- **قواعد الكوبونات** (الخصم على الخادم، العدّاد عند الاعتماد لا الإنشاء) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#قواعد-الكوبونات)
- **حرّاس تقرير الجودة** — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#حرّاس-تقرير-الجودة--ولماذا-كلٌّ-منها)
- **`killzone` — ثالث تكرار لنفس صنف العطل** (عمود موجود لا يُكتب) — `مطبَّق`. [تفصيل](DECISIONS_ARCHIVE.md#killzone--ثالث-تكرار-لنفس-صنف-العطل)

---

## قرارات سابقة ما زالت سارية

### `_WINRATE_OVERRIDE = 0.50` لا يُرفع قبل عينة موثوقة
**الحالة:** `مطبَّق` — `app/services/ai_engine_v5.py:970`

`get_winrate()` تُعيد قيمة ثابتة محايدة بدل أي رقم تاريخي، فلا تصل نتائج
قاعدة البيانات إلى معايرة المحرك. **شرط الرفع:** 50+ قراراً محسوباً من
**2026-09-16** فصاعداً (كان W35 قبل اكتشاف أعطال حلقة الرصد).

### `decision_grouping` هو المصدر الوحيد لكل تجميع
**الحالة:** `مطبَّق` — `app/services/decision_grouping.py`

القرار الواحد يُحفظ صفاً لكل مستخدم استلمه، فالعدّ الخام يضخّم كل شيء
بمعامل عدد المستخدمين. كل تقرير ومؤشر يمرّ بـ`verified_unique_decisions()`.

### محرك ICT هو المصدر الوحيد للاتجاه والمستويات
**الحالة:** `مطبَّق`

Gemini يُستخدم **لإثراء النص فقط**. أي خلل في Gemini (429/404) لا أثر له
على نسبة الربح أو جودة الإشارة.

### لا يُشغَّل Docker nginx
**الحالة:** `مطبَّق`

`system nginx` على السيرفر يتولى المنفذ 80/443 وشهادات SSL.
