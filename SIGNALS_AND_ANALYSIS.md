# خارطة نظام التحليل والتوصيات والإشارات — Qaffel AI

مرجع تقني كامل لكل جزء بالمنصة له علاقة بـ: التحليل، اتخاذ القرار (BUY/SELL/WAIT)،
حساب مستويات الدخول/الوقف/الهدف، حفظ الإشارات، وبثّها للمستخدمين. الهدف: تقدر
تراجع/تعدّل أي جزء بثقة، بدون ما تحتاج تقرأ آلاف الأسطر من الصفر.

---

## 1. الصورة الكبيرة — مسار إشارة واحدة من البداية للنهاية

```
بيانات سوق خام (smart_data.py)
        │
        ▼
ict_engine.py :: full_analysis()
   → BOS/CHoCH, Order Blocks, FVG, Liquidity Sweep,
     Premium/Discount, Kill Zone, Wyckoff, RSI/MACD/EMA
   → bull_score / bear_score (نظام نقاط تقاطع، رمز بالقسم 3)
        │
        ▼
ai_engine_v5.py :: analyze_market()
   → يستدعي ict_engine على LTF + HTF بالتوازي
   → _decision_finalizer()      → يقرر BUY/SELL/WAIT (القسم 4)
   → _institutional_gate()      → قواعد رفض صارمة (القسم 5)
   → _output_safety_gate()      → طبقة أمان إضافية
   → _confidence_calibration_layer() → يحسب % الثقة النهائي
   → _smart_rr_recovery() / rescue layers → يحاول ينقذ صفقات RR ضعيف (القسم 6)
   → يرجع dict analysis كامل: recommendation, levels{entry,sl,tp1,tp2}, confidence...
        │
        ▼
   ثلاث نقاط دخول مختلفة تستدعي analyze_market() وتقرر تحفظ الإشارة ولا لأ:
   ├─ backend/app/api/bot.py :: bot_analyze()         (طلب /analyze يدوي من أي مستخدم)
   ├─ telegram-bot/bot.py :: monitor_watchlists()     (فحص Watchlist كل 15 دقيقة تلقائياً)
   └─ backend/app/services/strategy_checker.py        (Strategy Builder، كل 5 دقائق)
        │
        ▼
   Signal row تُحفظ بجدول signals (القسم 7) — مع قاطع أمان الخسائر المتتالية (القسم 8)
        │
        ▼
   telegram-bot/bot.py :: broadcast_new_signals()  → بث الإشارة للمشتركين (القسم 9)
        │
        ▼
   telegram-bot/bot.py :: monitor_watchlists() (نفس اللوب، جزء check-outcomes)
   → يفحص كل إشارة ACTIVE إذا لمست TP1/TP2/SL → يحدّث status ويرسل نتيجة (القسم 10)
```

---

## 2. الملفات الرئيسية ودور كل وحدة

| الملف | الدور |
|---|---|
| `backend/app/services/smart_data.py` | مزود البيانات — أسعار حية + شموع تاريخية، fallback: yfinance → Finnhub → TradingView (خليجي) → TwelveData. فيه `is_market_open()` (نسخة الباك اند). |
| `backend/app/services/ict_engine.py` | يحسب كل مفاهيم ICT/SMC الخام من الشموع (BOS/CHoCH, OB, FVG, Sweep, Premium/Discount, Kill Zone, Wyckoff) + نظام نقاط bull/bear score (`_calculate_confluence_score`, حوالي سطر 790). |
| `backend/app/services/ai_engine_v5.py` | **المحرك الرئيسي** — يجمع كل شي، يقرر الاتجاه، يحسب المستويات، يحسب الثقة. أكبر وأهم ملف بالنظام (~3800 سطر). |
| `backend/app/api/bot.py` | Endpoints يستخدمها بوت تلغرام فقط (تتحقق بـ `BOT_SECRET`). فيه `bot_analyze()` (تحليل يدوي + حفظ تلقائي) و`save-alert-signal` (حفظ إشارة من فحص Watchlist). |
| `telegram-bot/bot.py` | عملية Python منفصلة بالكامل. فيها كل مهام الخلفية: بث الإشارات، فحص Watchlist، فحص نتائج TP/SL، تنبيهات الجلسات. **نسخة `is_market_open()` مستقلة عن الباك اند — لازم تحدّث الاثنين معاً.** |
| `backend/app/services/strategy_checker.py` | مهمة خلفية منفصلة لـ Strategy Builder (تفصيل بالقسم 11). |
| `backend/app/models/signal.py` | جدول `signals` — كل إشارة محفوظة (شخصية أو عامة). |

---

## 3. نظام النقاط (bull_score / bear_score) — `ict_engine.py` سطر ~790

كل معيار يضيف نقاط لصف واحد بس (صعودي أو هابط)، **بشكل متماثل تماماً بين الاتجاهين**
(نفس عدد النقاط لنفس قوة الإشارة، سواء صعودي أو هابط):

| المعيار | النقاط | الشرط |
|---|---|---|
| Market Structure (BOS) | 20 | اتجاه الهيكل صاعد/هابط |
| Order Block | 25 | السعر داخل OB صاعد/هابط |
| Fair Value Gap | 15 (أو 8 لو قريب) | داخل FVG أو قريب منه <0.5% |
| Premium/Discount Zone | 10-15 | حسب قوة المنطقة (EXTREME أم لأ) |
| Liquidity Sweep + رفض | 25 (أو 15 لو الجودة مش STRONG، أو 5 لو بدون رفض مؤكد) | أهم معيار — أساس ICT |
| Kill Zone | 10 (أو 5) | وقت تداول مؤسسي نشط |
| Wyckoff | 10 (أو 6) | مرحلة Wyckoff تدعم الاتجاه |
| RSI | 5 | تشبع بيعي/شرائي (<30 أو >70) |
| MACD | 5 | تقاطع صاعد/هابط مؤكد |

- **عقوبة**: لو ما في أي اكتساح سيولة إطلاقاً (`sweep_quality == NONE`) → -8 نقطة من الاتجاهين.
- **القرار الأولي**: الاتجاه الأعلى نقاطاً يفوز، بس لازم `confidence >= 40` و**3 عوامل على الأقل** مجتمعة، وإلا `WAIT`.

✅ **تحقّقنا (14/8/2026)**: هالجزء تحديداً سليم 100% ومتماثل — مش هو مصدر أي انحياز صعودي.

---

## 4. حسم القرار النهائي — `ai_engine_v5.py :: _decision_finalizer()` (سطر ~1663)

بعد نظام النقاط، القرار يمر بترتيب أولويات (كل وحدة ممكن "تحذّر" أو "تلغي" حسب قوتها):

1. **Score delta** (`bull_score - bear_score`) — المرجع الأساسي للاتجاه (`score_dir`)
2. **Market Structure** (BOS/CHoCH) — لو يعارض score_dir وdelta مش قوي (`< 40`) بسوق رينج → تحذير بس. بسوق متجه → الهيكل يقلب القرار.
3. **HTF Bias** (اتجاه الفريم الأعلى) — 🔴 **شُغل عليه إصلاح جذري بتاريخ 14/8/2026** (تفاصيل بالقسم 4.1 تحت)
4. **Liquidity sweep direction** — معلومة تشخيصية
5. **Premium/Discount zone** — لو BUY بمنطقة Premium (أو SELL بـ Discount) وdelta ضعيف → عقوبة ثقة 12 نقطة فقط

الثابت المهم: `self._DECISION_THRESHOLD_STRONG = 40` (سطر ~1661) — أي delta بهالقوة أو أعلى
"يكسر" أي تعارض مع الهيكل أو الفريم الأعلى ويسمح للصفقة تكمل رغم التعارض.

### 4.1 — إصلاح تعارض الفريم الأعلى (HTF Conflict) — 14/8/2026

**قبل الإصلاح**: لو الفريم الأعلى (مثلاً 4 ساعات) هابط، والفريم الصغير (15 دقيقة) يعطي
إشارة شراء محلية، الكود كان بس يخصم نقاط ثقة (`_htf_conflict_penalty`) **بدون ما يمنع
الصفقة إطلاقاً** — تعليق الكود القديم كان يقول حرفياً: *"HTF conflict is a multiplier
only... NEVER rejection"*.

**النتيجة الفعلية**: صفقات BUY كانت تتفتح عكس اتجاه الفريم الأعلى الهابط بثقة عالية،
وتنضرب بالـ SL بشكل متكرر — هاد بالضبط ما صار مع XAUUSD بتاريخ 13/8/2026 (~20 صفقة
BUY متتالية، شبه كلها SL_HIT، ولا صفقة SELL وحدة بالمنتصف).

**الإصلاح**: لو الفريم الأعلى يعارض القرار المحلي **و** الـ delta مش قوي كفاية
(`abs(score_delta) < 40`) → الصفقة تُرفض بالكامل وتتحول لـ `WAIT` (مو مجرد خصم ثقة).
لو الـ delta قوي فعلاً (`>= 40`) → الصفقة تُسمح بالمرور (انعكاس حقيقي مبرر) بس بعقوبة
ثقة زي القديم.

```python
# ai_engine_v5.py — داخل _decision_finalizer()
if htf_bias and htf_bias != resolved:
    strong_override = abs(score_delta) >= self._DECISION_THRESHOLD_STRONG
    if not strong_override:
        analysis["htf_conflict_reject"] = True   # → recommendation يصير WAIT بالقسم "Commit decision"
    else:
        ...عقوبة ثقة متلما كانت...
```

**أثر متوقع**: عدد إشارات أقل إجمالاً (خصوصاً بالأسواق المتذبذبة عكس الاتجاه الأعلى)،
بس نسبة نجاح أعلى — لأنه ما رح تفتح صفقات تحارب اتجاه أكبر إلا لو كانت قوية فعلاً.

⚠️ **هاد تغيير بمحرك القرار الأساسي — بيأثر على كل الأزواج، مو بس الذهب.** يحتاج مراقبة
أداء لأسبوع/أسبوعين قبل ما نحكم إذا لازم ضبط أدق (مثلاً تخفيف العتبة لـ 35 لو صار الإشارات قليلة جداً).

---

## 5. البوابة المؤسسية (Institutional Gate) — `_institutional_gate()` (سطر ~2170)

قواعد رفض إضافية (Rules 1-16) تعمل **بعد** ما يتقرر BUY/SELL. أغلبها "تحذير فقط"
(soft warning)، مو رفض صريح — الفلسفة العامة بالكود: "ICT Engine هو المصدر الوحيد
للاتجاه، أي شرط يفشل → تحذير أو خصم ثقة، مش رفض قاطع" إلا لقواعد قليلة صارمة فعلاً:
- RR أقل من الحد الأدنى (`ZERO_SL`, `RR_...`)
- SL بالاتجاه الغلط (`BUY_SL_ABOVE`, `SELL_SL_BELOW`)
- TP بالاتجاه الغلط (`BUY_TP1_BELOW`, `SELL_TP1_ABOVE`)

⚠️ **ملاحظة تقنية**: لقينا تعريفين لدالة `_institutional_gate` بنفس الملف (سطر 2054
و2170) — بايثون بياخذ التعريف الثاني بس (الأول كود ميت غير مستخدم إطلاقاً). مش خطأ
مؤثر حالياً (الدالة الفعالة صحيحة)، بس يستاهل تنظيف لاحقاً لتجنب لخبطة أي تعديل مستقبلي
على النسخة الغلط.

---

## 6. إنقاذ الصفقات ضعيفة RR — `_smart_rr_recovery()` (سطر ~3175)

لو صفقة عندها confluence قوي (`effective_delta >= 25`) بس RR ضعيف (تحت 1.0)، الكود
يحاول ينقذها بالترتيب:
1. **مد الهدف (TP)** لأقرب مستوى سيولة (BSL/SSL) لتحسين RR
2. **استخدام حدود Order Block** كهدف بديل لو أبعد من TP الحالي
3. **تضييق SL** لحدود الـ OB (بس لو BOS مؤكد — الهيكل لسا صالح)
4. **استثناء الـ Delta العالي**: قبول RR ≥ 1.0 (كلا الحالتين — الكود فيه تعليق قديم
   يقول "0.8 لـ delta≥30، 0.9 لـ delta≥25" بس القيم الفعلية بالكود ثابتة عند 1.0 للحالتين
   — **تناقض بين التعليق والكود يستاهل مراجعة لو حابب تشدد/تخفف هالشرط**)

هاد الجزء **لا علاقة له بتحديد الاتجاه** (BUY أو SELL) — بس بيتحكم هل نسمح للصفقة
تكمل بعد ما تقررت، أو نرفضها لضعف نسبة المخاطرة/العائد.

---

## 7. أين تُحفظ الإشارة فعلياً بقاعدة البيانات

ثلاث نقاط حفظ منفصلة، كلهن بيكتبن لنفس جدول `signals`:

### أ) `backend/app/api/bot.py :: bot_analyze()` — تحليل يدوي
يشتغل لما أي مستخدم يطلب `/analyze` من البوت مباشرة. شروط الحفظ:
`rec in (BUY,SELL)` + `confidence >= 40` + `RR >= 1.2` + مش مرفوضة + مش بكولداون +
السوق مفتوح + **قاطع أمان الخسائر المتتالية (جديد، القسم 8)**.

### ب) `backend/app/api/bot.py :: save-alert-signal` — تنبيه Watchlist
يستدعيه `telegram-bot/bot.py :: monitor_watchlists()` (كل 15 دقيقة) لكل مستخدم عنده
Watchlist، لكل رمز فيها. نفس شروط الحفظ + **قاطع أمان الخسائر المتتالية**. هاد المصدر
يلي أنتج صفقات "admin" و"acc.a7med.mosa" وغيرهم يلي شفناها بلوحة الإدارة.

### ج) `backend/app/services/strategy_checker.py` — Strategy Builder
منفصل تماماً — يحفظ بجدول `strategy_trigger_events` (مو `signals`)، ومنطقه مختلف
بالكامل (تقييم شروط يبنيها المستخدم بنفسه، مش قرار المحرك الرئيسي). تفصيل بالقسم 11.

---

## 8. قاطع أمان الخسائر المتتالية (جديد — 14/8/2026)

`backend/app/api/bot.py :: _check_loss_streak_breaker()` — طبقة حماية **مستقلة تماماً
عن محرك القرار**، تعمل قبل أي حفظ إشارة جديدة (بالمصدرين أ وب أعلاه):

- يفحص آخر **3 إشارات** بنفس الزوج + نفس الاتجاه (BUY أو SELL)
- لو الثلاثة كانوا `SL_HIT` والأحدث منهم صار قبل أقل من **6 ساعات** → يوقف حفظ/بث أي
  إشارة جديدة بنفس الزوج/الاتجاه لحد ما تنتهي فترة التهدئة
- الثابتين قابلين للتعديل مباشرة بأعلى الملف: `_LOSS_STREAK_LIMIT = 3`,
  `_LOSS_STREAK_COOLDOWN_H = 6`

هاد أسرع خط دفاع — بيوقف النزيف فوراً بغض النظر عن سبب الخسارة الجذري (سواء كان تعارض
فريم أعلى، أو أي سبب تاني ما اكتشفناه بعد).

---

## 9. بث الإشارات — `telegram-bot/bot.py :: broadcast_new_signals()`

لوب خلفي منفصل (كل 60 ثانية):
1. يجيب كل إشارة `broadcast_sent=False` من `/api/v1/bot/new-signals`
2. **قفل اتجاه** (`DIRECTION_LOCK_MIN = 120` دقيقة): لو آخر بث لنفس الرمز كان **بنفس
   الاتجاه** خلال 30 دقيقة (`ALERT_COOLDOWN`) → تخطي. لو **باتجاه معاكس** خلال ساعتين
   → تخطي كمان (منع تضارب رسائل متناقضة بفترة قصيرة)
3. يرسل لكل مشترك نشط (أو بس يلي عندهم الرمز بـ Watchlist لو محددين وحدة)

⚠️ **نقطة يستاهل الانتباه لها**: هالقفل شغّال على مستوى "البث العام" — يعني حتى لو
المحرك حسب SELL بلحظة معينة، لو آخر بث كان BUY قبل أقل من ساعتين، الـ SELL ما رح
يتبث. هاد ممكن يفسر جزء من نمط "BUY بس" يلي شفناه — إذا بعد إصلاح HTF (قسم 4.1) استمر
نفس النمط، هاد أول مكان نرجع نفحصه.

---

## 10. متابعة النتائج (TP/SL) — `telegram-bot/bot.py :: monitor_watchlists()`

نفس اللوب (كل 15 دقيقة)، أول خطوة فيه: يجيب `/api/v1/bot/check-outcomes` (الباك اند
يفحص كل إشارة `ACTIVE` مقابل السعر الحي، يحدّث لـ `TP1_HIT`/`TP2_HIT`/`SL_HIT` حسب
الأسبق)، ويرسل رسالة نتيجة لصاحب كل إشارة.

---

## 11. Strategy Builder — نظام منفصل بالكامل

لتذكير: هاد **مو** نفس نظام "الإشارات" أعلاه — نظام مستقل بالكامل يبنيه المستخدم بنفسه
(شروط مخصصة، AND/OR/AT_LEAST، رموز مختارة يدوياً). التفاصيل:

- `backend/app/models/strategy.py` — الموديلات
- `backend/app/services/strategy_engine.py` — تقييم الشروط (`evaluate_strategy`,
  `SUPPORTED_CONDITION_TYPES` — قائمة الأنواع المدعومة فعلياً بالمحرك الحقيقي)
- `backend/app/services/strategy_checker.py` — الفحص الدوري (كل 5 دقائق) + قفل
  Postgres advisory lock (يمنع تكرار الفحص عبر الـ 2 worker processes)
- يستخدم **نفس** `mosh_ai_engine_v5.analyze_market()` لجلب بيانات السوق، بس القرار
  النهائي (تحقق الاستراتيجية أو لأ) مبني على شروط المستخدم فقط، مو على
  `recommendation`/BUY-SELL-WAIT من المحرك الرئيسي مباشرة.

---

## 12. جدول الثوابت القابلة للتعديل (Tuning Knobs)

| الثابت | الموقع | القيمة الحالية | يتحكم بـ |
|---|---|---|---|
| `_DECISION_THRESHOLD_STRONG` | `ai_engine_v5.py` ~1661 | 40 | العتبة اللي فوقها الـ delta "يكسر" أي تعارض هيكل/فريم أعلى |
| `_DELTA_EXCEPTION` | `ai_engine_v5.py` ~3186 (داخل `_smart_rr_recovery`) | 25 | أقل delta يسمح بمحاولة "استثناء" لصفقة RR ضعيف |
| `_RR_HARD_FLOOR` / `_RR_FLOOR_HIGH` | نفس الدالة | 1.0 / 1.0 | أقل RR مقبول بعد محاولات الإنقاذ |
| `min_rr` بالحفظ التلقائي | `bot.py :: bot_analyze/save-alert-signal` | 1.2 | أقل RR يسمح بحفظ الإشارة أصلاً |
| `conf >= 40` | نفس المكان | 40% | أقل ثقة يسمح بحفظ الإشارة |
| `_LOSS_STREAK_LIMIT` / `_LOSS_STREAK_COOLDOWN_H` | `bot.py` (جديد) | 3 / 6 ساعات | قاطع أمان الخسائر المتتالية |
| `ALERT_COOLDOWN` / `DIRECTION_LOCK_MIN` | `telegram-bot/bot.py` | 30 / 120 دقيقة | فترة الصمت بين تنبيهات نفس/عكس الاتجاه |
| `MONITOR_INTERVAL` | `telegram-bot/bot.py` | 900 ثانية (15 دقيقة) | دورية فحص الـ Watchlist |
| `CHECK_INTERVAL_SEC` | `strategy_checker.py` | 300 ثانية (5 دقائق) | دورية فحص Strategy Builder |

---

## 13. نقاط لسا تستاهل مراجعة (لو حابب نكمل الحفر)

1. **قفل اتجاه البث** (قسم 9) — ممكن يكتم SELL حقيقي لو آخر بث كان BUY قبل أقل من ساعتين.
2. **تناقض التعليق مع القيم الفعلية** بـ `_smart_rr_recovery` (قسم 6) — القيم أصرم مما يقول التعليق.
3. **تعريف مكرر لـ `_institutional_gate`** (قسم 5) — كود ميت، تنظيف لاحق.
4. **مراقبة أداء إصلاح HTF** (قسم 4.1) لمدة أسبوعين — نتأكد إنه قلل الخسائر المتتالية فعلاً بدون ما يخنق عدد الإشارات بشكل مبالغ فيه.
